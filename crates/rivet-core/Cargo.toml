[package]
name = "rivet-core"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Owned extraction records, symbols, uses, imports, scopes, spans, errors"

[dependencies]
serde.workspace = true
toml.workspace = true
