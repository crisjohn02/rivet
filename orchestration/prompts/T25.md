Implement T25 from docs/TASKS.md only. Read AGENTS.md first, then the T25 row; spec §16.2 (Collapsed Form) and §10.1; `docs/OUTPUT-CONTRACT.md` "Coordinates and symbol objects" (the `kind` enum line is normative); `docs/ADDING-A-LANGUAGE.md` "MVP support boundary" rows for named definitions, qualified names, and not-resolved-in-v0.1; `crates/rivet-languages/src/php/` (`mod.rs`, `signature.rs`, `symbols.scm`); `tests/fixtures/php/authored/`; `tests/gold/php-authored.toml` and `tests/gold/check_gold.py`; and `crates/rivet-languages/tests/php_extract.rs`. Nothing else.

`docs/OUTPUT-CONTRACT.md` is normative. Where this prompt and the contract disagree, the contract wins and you report the disagreement.

T25 — Finish the remaining MVP PHP definition kinds and collapsed signatures.

Start from what already exists; this task is smaller than it looks and most of it is coverage. `symbols.scm` already captures namespaces, classes, interfaces, traits as class-kind, enums, functions, methods, properties and constants, and `mod.rs` already walks multi-element property and const declarations. The genuine gaps are enum cases, fixture and gold coverage for the kinds that exist but are never exercised, and the member-summary builder's kind coverage. Do not rewrite the extractor.

Requirements:

1. Enum cases. They are currently not extracted at all; there is no `enum_case` capture anywhere. The contract's `kind` enum has no enum-case value, so decide between `const` and not extracting them, and justify the choice against the contract and `docs/ADDING-A-LANGUAGE.md`'s "Named definitions" row, which does not name them. If you choose `const`, note that `lookup_name` keeps `Const` case-sensitive, which is correct for PHP, and say so. Handle both pure and backed enums.

2. Fixture additions. The authored fixture has no interface, no trait, no enum, no abstract class, no static or readonly member, no promoted constructor property, no multi-name declaration (`public $a, $b;` or `const A = 1, B = 2;`) and no top-level `const`. The multi-element walk in `mod.rs` is entirely untested today. Add focused fixture files covering these. Keep each file small and single-purpose; do not bloat the existing four files, because their spans are already in gold and you would have to renumber everything.

3. Gold entries for every new declaration. `tests/gold/php-authored.toml` requires `file`, `qualified_name`, `kind`, `start_byte`, `end_byte`, `start_line`, `end_line`, and `text`, where `text` is EXACTLY the first 40 bytes of the span. Compute these programmatically rather than by hand; a miscounted byte will fail `check_gold.py`. Note that `php_extract.rs::php_authored_declarations_match_gold` asserts both that every gold entry matches exactly one symbol AND that no extracted non-module symbol is unclaimed, so an addition on either side without the other fails loudly. That is the property you want; do not weaken that test.

4. Qualified names must follow the ADDING-A-LANGUAGE rules: namespace separator `\`, member separator `::`, and properties retain their `$`. Traits are class-kind. Verify an interface's bodyless method produces a sensible signature, since `header()` drops the trailing semicolon when there is no body.

5. Collapsed signatures. `signature_summary` in `signature.rs` currently emits member lines only for `Property`, `Const` and `Method`, and its own comment names T25 as the task that extends kind coverage. Extend it so a container summary lists every extracted member kind without bodies, including enum cases if you extract them, and interface methods. Bodies collapse to `{ … }` for methods; non-method members end with `;`.

6. Do NOT wire `signature_summary` into command output. It currently has no callers outside its own unit tests, and its consumer is the context builder in T28 onwards. Making it correct and tested is this task; plumbing it to output is not. Say in your report that you left it unwired deliberately.

7. Promoted constructor properties are `property_promotion_parameter` nodes, not `property_declaration`. Decide whether they are in scope for extraction as properties, justify against the MVP boundary table, and state the decision either way rather than leaving it ambiguous.

8. Anonymous containers must stay excluded. A previous review caught anonymous-class members leaking as symbols. Add or keep a test proving members of an anonymous class in your new fixture files are not extracted.

9. Bump the `fact-schema` component in `EXTRACTOR_FINGERPRINT` if what the extractor records changes shape or meaning, per the rule T22 established. Say whether you bumped it and why.

10. Write the adapter README that `docs/ADDING-A-LANGUAGE.md` requires and that does not yet exist: `crates/rivet-languages/src/php/README.md`, documenting supported syntax and the constructs left unresolved in v0.1. Keep it short and factual. This is the one documentation file you may create; the usual no-docs rule still covers everything else.

11. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts and the new gold entry total.

12. Update docs/TASKS.md: tick T25, Last completed = T25, Next = T26, one-line "Last checks". A T21b row may be present and unticked; leave it alone, another session owns it. Do not touch other docs beyond the adapter README. Do not run git.

If this grows beyond one coherent change, split as T25a/T25b, record what works and what remains, and do not tick T25. Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
