Implement T26 from docs/TASKS.md only. Read AGENTS.md first, then the T26 row; spec §16 through §16.3 (§16.3 Ranking is the governing section), §12.2 (the relationship vocabulary paragraph), §11.3, and §20.2; `docs/OUTPUT-CONTRACT.md` the `rivet context` section and the flag-applicability rows for context; `docs/ARCHITECTURE.md` "Resolution and context"; `crates/rivet-cli/src/references.rs`, `symbol.rs`, `transport.rs`; `crates/rivet-core/src/config.rs` `ContextConfig`; and `crates/rivet-store/src/lib.rs` read APIs. Nothing else.

`docs/OUTPUT-CONTRACT.md` is normative for JSON and the spec is normative for ranking. Where this prompt disagrees with either, they win and you report the disagreement.

T26 — Collect direct context candidates and apply the fixed integer ordering. This is candidate collection and ranking ONLY. Depth two, traversal caps, source estimation, budget fitting, overlap suppression and command wiring all belong to T27 through T30. Do not build them.

Requirements:

1. Where the code lives. Put it in a new `crates/rivet-cli/src/context.rs` and declare the module. `references.rs` is `pub(crate)` in `rivet-cli`, so a sibling module can reuse it directly while `rivet-index` cannot. Note in your report that `crates/rivet-index/src/lib.rs`'s module doc claims this crate owns context ranking; report that discrepancy, do not fix it.

2. Do NOT wire the command. `main.rs` routes `Context` to `not_implemented` naming T30, and `crates/rivet-cli/tests/index_json.rs` asserts that. Leave both alone. Your tests therefore drive `rivet index` through the binary and then call your collection function against a `Store` opened directly, which is the established pattern in `tests/bindings.rs` and `tests/reresolve.rs`. Say in your report that you left the wiring to T30 deliberately.

3. Produce candidates with the reasons spec §16.3 names, at depth 1 only: `target`, `type`, `callee`, `test`, `caller`, `import`, `parent`. `second_degree` is depth 2 and belongs to T27; do not emit it.

4. Order by the spec's ascending tuple exactly: `(depth, reason_priority, resolution_priority, file_utf8_bytes, start_byte, id)` with `target=0, type=1, callee=2, test=3, caller=4, import=5, parent=6, second_degree=7` and `exact=0, scoped=1, name_match=2`. The target is always first. Use the spec's integers; do not invent your own. Deduplicate by symbol ID retaining the BEST tuple, so a symbol reachable as both a callee and a caller keeps the callee ranking.

5. Reuse `references.rs` rather than writing a second matcher. Callees are contained call matches followed to their binding targets; callers are reference-mode call matches followed to their containing symbols. These are exactly the two lists `symbol` already builds.

6. The import reason is the subtlest part of this task; read it twice. Spec §16.3 says to use "only direct imports consumed within the candidate symbol, not every dependency in its file". A PHP import use sits at file scope, so its containing symbol is null and it is NOT contained by the target. Selecting import-kind uses contained by the target therefore yields nothing, and selecting every import in the file is explicitly forbidden. Derive it instead: take the uses inside the target, follow their bindings, and keep those reached through a file-level import binding. State exactly how you implemented it and which fixture import it resolves.

7. A caller whose use has no containing symbol, such as a top-level call, yields no caller candidate. The fixture has two of these in `boot.php`. Skip them; do not crash and do not invent a container. Assert this.

8. Name-only links. Spec §16.3 says "name-only links do not expand context by default", while `resolution_priority` includes `name_match=2`. Read §16.3 and decide whether a name-only link produces a depth-1 candidate at all or only fails to open a further hop. State your reading and why, and make the behavior match it. `ReportService::runUnknown` calls `launch()` with no binding, which is the case that distinguishes the two readings.

9. The `test` reason comes from a caller whose file matches the configured test globs in `ContextConfig`. Nothing in the authored fixture matches them, so write a temp fixture with a file under `tests/`. Note that a test-glob caller is a source relationship, never a claim of coverage or execution; keep that wording out of any message you write.

10. Determinism. No `HashMap` iteration order may reach output. Sort explicitly. The same input must produce the identical candidate list every time, and a test must assert that by running collection twice and comparing.

11. Tests, in a new `crates/rivet-cli/tests/` file following the shape of `refs_json.rs` and `symbol_calls_json.rs`, with its own temp-directory prefix. Assert concrete symbol IDs and the full ordered reason sequence, not just counts. Cover each reason with a real fixture relationship: `SurveyService::launch` has three in-fixture callers and a parent; `ReportService::runTyped` has a callee and a type; `ReportService.php` imports the survey class twice, once aliased; `App\Constants\FIRST` has no parent, which must be absent rather than an error. Also cover deduplication where one symbol is reachable two ways, the null-container caller, the name-only case from item 8, and determinism.

12. `tests/fixtures/php/authored/README.md` is stale: it still says three files and predates the five added in T25. Update it to describe the current fixture. That file and `docs/TASKS.md` are the only documentation you may edit.

13. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts.

14. Update docs/TASKS.md: tick T26, Last completed = T26, Next = T27, one-line "Last checks". Do not run git.

If this grows beyond one coherent change, split as T26a/T26b, record what works and what remains, and do not tick T26. Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
