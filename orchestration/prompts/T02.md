Implement T02 from docs/TASKS.md only. Read AGENTS.md first, then the T02 row, docs/BUILDING.md "Language features" and "Prerequisites", docs/ADDING-A-LANGUAGE.md "MVP support boundary" (Files row only) and "Implementation checklist" (first two bullets), and docs/IMPLEMENTATION-PLAN.md "Dependency and packaging decisions". Nothing else.

T02 — Pin compatible Tree-sitter and grammar dependencies; wire language features; add grammar smoke tests.

Requirements:
1. Discover current versions with `cargo search tree-sitter`, `cargo search tree-sitter-php`, `cargo search tree-sitter-typescript` (or `cargo add --dry-run`). Choose a `tree-sitter` core version and grammar crate versions that are mutually compatible (grammar crates must link against the same `tree-sitter-language` ABI the core expects). Pin each with an exact `=x.y.z` requirement in `[workspace.dependencies]`. If the latest versions are incompatible, step back to the newest compatible set and record which versions you rejected and why.
2. Features live in `rivet-languages`: `lang-php` and `lang-typescript` (the latter enables both the TypeScript and TSX grammar variants from the `tree-sitter-typescript` crate). `default = ["lang-php", "lang-typescript"]`. `rivet-parser` depends on `rivet-languages` and forwards features of the same names; `rivet-cli` depends on `rivet-parser` and forwards them again with the same default. No other new inter-crate dependencies.
3. In `rivet-languages`, add the minimum code needed to expose the grammars: a `pub enum LanguageId { Php, Typescript, Tsx }` (only variants whose feature is enabled) and a `pub fn grammar(id: LanguageId) -> tree_sitter::Language`. No extraction, no trait, no queries. Doc-comment both.
4. Smoke tests in `crates/rivet-parser/tests/grammar_smoke.rs`, each behind `#[cfg(feature = ...)]`: parse a five-line PHP class with one method, a five-line TypeScript class, and a five-line TSX function returning JSX. Assert `!root.has_error()` and that the root node kind is the grammar's program/source node. Include one deliberately malformed PHP snippet and assert `root.has_error()` is true, to prove error detection works for the T11 parse policy.
5. Prove all three builds and paste the real last line of each:
   cargo build -p rivet-cli --no-default-features
   cargo build -p rivet-cli --no-default-features --features lang-php
   cargo build -p rivet-cli
   With no features, `rivet --version` must still run.
6. Set `rust-version` in `[workspace.package]` to the highest `rust-version` declared by any dependency in the resolved graph (inspect with `cargo metadata --format-version 1 | python3 -c ...` or by reading `cargo tree`), never higher than the installed 1.98.1. Record the value and how you derived it in the TASKS checkpoint "Last checks".
7. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace` (default features) and `cargo test -p rivet-parser --no-default-features --features lang-php`. Paste real result lines.
8. Update docs/TASKS.md: tick T02, set Last completed = T02, Next = T03, and record pinned versions plus rust-version in "Last checks". Do not touch other docs. Do not run git.

Do not add any dependency other than tree-sitter and the two grammar crates. Do not create scanner, store, or CLI subcommand logic. Finish with the AGENTS.md final block.
