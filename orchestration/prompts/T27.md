Implement T27 from docs/TASKS.md only. Read AGENTS.md first, then the T27 row; spec §16.3 (Ranking) and §16.4 item 4 (the traversal caps), plus §16.5 (Options); `docs/OUTPUT-CONTRACT.md` the `rivet context` section and the context flag-applicability rows; `crates/rivet-cli/src/context.rs` (T26) and `crates/rivet-cli/tests/context_rank.rs`; `crates/rivet-cli/src/references.rs`; and `crates/rivet-core/src/config.rs` `ContextConfig`. Nothing else.

The spec is normative for ranking and traversal, and `docs/OUTPUT-CONTRACT.md` for the JSON field names. Where this prompt disagrees with either, they win and you report the disagreement.

T27 — Add depth-two traversal, relationship flags, and the fixed work caps. Source estimation, budget fitting and overlap suppression are T28 and T29; command wiring is T30. Do not build them, and leave `main.rs` routing `context` to `not_implemented` naming T30.

Requirements:

1. API. Extend `context.rs` with an options type carrying `depth` (1 or 2), `include_tests`, `include_callers`, `include_callees`, and the two caps, and a collection function that returns both the ranked candidates and a `candidate_limit_reached` flag. Name the flag exactly as the contract does. Keep T26's `collect_candidates` working: with depth 1 and every relationship included, the new function must return exactly T26's list, and a test must assert that equality on the fixture.

2. Depth two. From each depth-1 candidate, visited in tuple order, follow its own type, callee, caller and import links. Every depth-2 candidate has reason `second_degree`, and its resolution is the WEAKEST link on the path, meaning the worse of the two hops' resolutions. Deduplicate by ID across both depths retaining the best tuple, so a symbol reachable at depth 1 never also appears as `second_degree`, and the target never reappears.

3. What does not expand. State your reading of each, and make the behavior match:
   a. Name-only links. T26 decided that a `name_match` link yields a depth-1 candidate but never seeds depth 2. Keep that, and also decide whether a `name_match` link found at depth 2 produces a candidate. Spec §16.3 says name-only links "do not expand context by default".
   b. Containment. Spec §16.3 says "No sibling expansion through containment" and that parent "is also eligible at depth 1". Do not expand from a parent candidate into its other members, and do not add a parent of a depth-1 candidate at depth 2, unless you find the spec requires it, in which case say so.

4. Relationship flags, applied BEFORE traversal. Spec §16.3: "disabled relationships cannot reappear through another hop". Excluding callers removes caller links at both depths, so a symbol reachable only through a caller edge is absent even if a second hop would reach it. The same holds for callees. Excluding tests removes every candidate reached through a caller edge from a file matching the test globs, at both depths. Defaults: callers and callees included; tests from `ContextConfig.include_tests`; depth from `ContextConfig.max_depth`.

5. Caps. Spec §16.4: at most 1,000 unique candidates and 10,000 examined uses; traverse frontier symbols in tuple order and uses by `(file, start_byte, end_byte, ref_kind)`; "stop before exceeding a cap and report `candidate_limit_reached`". Make the caps fields of the options type whose defaults are exactly 1,000 and 10,000, so tests can use small values. Define precisely what counts as an examined use, since the current implementation scans whole tables, and write that definition in a doc comment. The unique candidate count, including the target, must never exceed the cap.

6. The flag must be truthful in both directions. It is true only when a cap actually stopped traversal with work remaining, and false otherwise. The boundary is the adversarial case: a graph that yields EXACTLY the cap's number of candidates with nothing left over must report false, and one more candidate must report true. Test both.

7. Termination and determinism. A call cycle, A calls B calls A, must terminate and produce each symbol once. Which candidates survive a cap must be fully determined by traversal order, so running the same capped collection twice yields the identical list.

8. Tests, extending `crates/rivet-cli/tests/context_rank.rs` or a new sibling file; say which. Assert concrete IDs and full ordered sequences, not counts alone. Cover: a depth-2 candidate with its `second_degree` reason and weakest-link resolution; a symbol reachable at both depths appearing once at depth 1; the target never reappearing; each flag excluding its relationship at both depths, including a symbol that would otherwise arrive by a second hop; the name-only and containment readings from item 3; the cycle; the exact-cap and cap-plus-one boundary for the candidate cap and separately for the examined-use cap; and one test at the real default of 1,000 candidates using a generated temp fixture, proving the default cap holds at scale.

9. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts.

10. Update docs/TASKS.md: tick T27, Last completed = T27, Next = T28, one-line "Last checks". Do not touch other docs. Do not run git.

If this grows beyond one coherent change, split as T27a/T27b, record what works and what remains, and do not tick T27. Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
