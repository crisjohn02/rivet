Implement T17 from docs/TASKS.md only. Read AGENTS.md first, then the T17 row and the "Read:" line for section D, spec §11.2 and §11.5 (last paragraph on interpolation), docs/ADDING-A-LANGUAGE.md "MVP support boundary" (PHP column, rows Candidate extraction and Not resolved), "Adapter contract", and "Query capture conventions"; tests/fixtures/php/authored/README.md and the `[[use]]` and `[[not_a_use]]` entries in tests/gold/php-authored.toml. Nothing else.

IMPORTANT: you are working in a git worktree on branch `task/T17`, in parallel with other tasks. Touch ONLY `crates/rivet-core/src/extract.rs` (additive types), `crates/rivet-languages/src/php/` (new file `uses.rs` or `uses.scm` plus small hooks in `mod.rs`), `crates/rivet-languages/tests/php_uses.rs` (new), and the T17 row plus checkpoint in `docs/TASKS.md`. Do not modify `rivet-cli`, `rivet-store`, `rivet-index`, or any other file. Do not run git.

T17 — PHP call/use span extraction with containing symbols. No resolution, no persistence.

Requirements:
1. Extend the owned records in `rivet-core::extract` additively: `ExtractedUse { spelling: String, ref_kind: RefKind, span: Span, containing_symbol_index: Option<usize> (index into ExtractedFile.symbols; None for top-level), receiver: Option<String> (source text of the receiver expression), scope_key: String, hint: UseHint }` and `UseHint` enum: `This`, `SelfOrStatic`, `NewExpr { class_spelling: String }`, `Typed { type_spelling: String }`, `Imported { binding: String }`, `Unresolved`. Add `uses: Vec<ExtractedUse>` and `imports: Vec<ExtractedImport { spelling_alias: String, target_qualified: String, kind: ImportKind (Class|Function|Const), span: Span }>` to `ExtractedFile` with `#[derive(Default)]` semantics preserved. T17 fills `hint` with only `This`, `SelfOrStatic`, `NewExpr` (when the receiver variable's most recent assignment in the same function body is `new X(...)`), `Typed` (when the receiver is a parameter with an explicit type, or a promoted/typed property accessed via `$this->prop`), otherwise `Unresolved`. Full binding resolution is T19–T21; here you only record lexical hints.
2. PHP use extraction covering, per §11.2/§11.5: function calls (`launch()` → `Call`, spelling `launch`), method calls (`$x->launch()` → `Call`, receiver `$x`), static calls (`Foo::bar()` → `Call`, receiver `Foo`), `new Foo(...)` (`Type`, spelling `Foo`), type positions in parameters/returns/properties (`Type`), `use` imports (`Import` spelling = imported name, and populate `imports`), class-constant and property accesses (`Read`), assignments to properties (`Write`). Anything else that is an identifier use but unclassified → `Unknown`. A call site appears exactly once as `Call`, never also as `Unknown`/`Read`.
3. Exclusions: identifiers inside comments and inside literal string portions are not uses; the expression inside an interpolation `"{$svc->launch()}"` IS a use with kind `Call`. Declaration names themselves (the `launch` in `function launch()`) are not uses. Variable names alone (`$svc`) are not uses.
4. `containing_symbol_index`: the innermost named symbol (method/function/class) whose span contains the use; uses inside closures/arrow functions attach to the nearest named container; top-level → `None`. `scope_key`: `"<file-order index of containing symbol or 'top'>:<function-body ordinal>"` or any deterministic string that uniquely identifies the lexical scope within the file and is stable across reparses of identical content.
5. Test `crates/rivet-languages/tests/php_uses.rs`: for every `[[use]]` in the gold, exactly one extracted use matches `(file, start_byte, end_byte, ref_kind)`; for every `[[not_a_use]]` span, NO extracted use overlaps it; the top-level `launch();` in `boot.php` has `containing_symbol_index == None`; the `$this->launch()` use has hint `This`; the `$svc->launch()` after `new` has hint `NewExpr { class_spelling: "\\App\\Services\\SurveyService" }` or the spelling as written; the typed-parameter call has hint `Typed { type_spelling: "SurveyService" }`; the `$x->launch()` has `Unresolved`; the interpolated call is present; the `use App\Services\SurveyService as SurveySvc;` import is recorded with alias `SurveySvc`. Report any extracted `Call` use not in gold as a test failure (the gold lists every launch call), but allow non-`launch` uses.
6. Run `cargo fmt --all -- --check`, `cargo clippy -p rivet-core -p rivet-languages --all-targets --all-features -- -D warnings`, `cargo test -p rivet-languages`, `cargo build -p rivet-cli --no-default-features`. Paste real results.
7. Update docs/TASKS.md: tick T17 only and set the checkpoint's "Active task / partial progress" line to mention T17 done on branch task/T17 (the orchestrator will reconcile the checkpoint on merge). Do not change "Last completed"/"Next". Do not run git.

Finish with the AGENTS.md final block.
