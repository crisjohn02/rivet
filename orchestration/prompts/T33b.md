Implement T33b from docs/TASKS.md only. Read AGENTS.md first, then the T33 and T33b rows and the paragraph beneath the T33 subtask table; `docs/AGENT-SNIPPET.md` in full; `docs/INTEGRATION.md`; spec §25 and §27; `docs/OUTPUT-CONTRACT.md` "Administrative commands" (the `init --json` shape, the `--snippet-file` paragraph, and the `snippet` output), "Flag applicability" and "Errors"; `crates/rivet-cli/src/init.rs` (T33a) and its tests `crates/rivet-cli/tests/init_json.rs`; and `crates/rivet-cli/src/main.rs`. Nothing else.

`docs/OUTPUT-CONTRACT.md` is normative for JSON and errors, and `docs/AGENT-SNIPPET.md` for the shipped text. Where this prompt disagrees with either, they win and you report the disagreement.

T33b — Implement `rivet snippet`, and `rivet init --write-snippet` with `--snippet-file`, which install or update the managed block in `AGENTS.md` or `CLAUDE.md`.

Requirements:

1. The shipped text is exact. `docs/AGENT-SNIPPET.md` says: "This managed block is exactly what `rivet snippet` prints (including markers and a final newline)", and "The benchmark's treatment uses exactly this text. Changes require a new recorded snippet hash." So the bytes matter beyond this task. Embed the block, from `<!-- rivet:start -->` through `<!-- rivet:end -->` plus one final LF, and add a test that extracts the fenced block from `docs/AGENT-SNIPPET.md` itself and asserts it equals `rivet snippet` output byte for byte, so the two can never drift. Report the SHA-256 of the shipped bytes in your final message.
2. `rivet snippet` prints exactly those bytes, ending in LF. `rivet snippet --json` returns `{"schema_version": 1, "snippet": "<the same bytes>"}` in that key order. It needs no repository: it must work in a directory that is not one, creating nothing.
3. Destination selection, per spec §25 and AGENT-SNIPPET "Installation behavior":
   - With `--write-snippet` and no `--snippet-file`: if exactly one of `AGENTS.md` and `CLAUDE.md` exists at the root, use it; if neither exists, create `AGENTS.md`; if both exist, fail with `invalid_arguments`, exit 2, naming both and telling the user to pass `--snippet-file`.
   - An explicit `--snippet-file AGENTS.md|CLAUDE.md` overrides auto-selection, even when the other file exists, and creates the selected file if missing. Only those two root-relative names are accepted in v0.1; anything else is exit 2. T33a already rejects `--snippet-file` without `--write-snippet`; keep that.
4. The managed block. Install it idempotently: if the file has no block, append it, separated from existing content by exactly one blank line and choosing a sensible form for an empty file; if it has exactly one well-formed block, replace only the bytes between and including the markers; if the block is already identical, change no byte and report nothing modified. Preserve every byte outside the block. Reject a malformed or duplicate block without writing, per AGENT-SNIPPET: "Existing duplicate/malformed markers are an error, not a reason to append another block." Malformed means a start with no end, an end with no start, an end before its start, or nested or repeated markers. Choose the error code for a malformed block from the contract, report which, and say why.
5. Line endings. Decide what to do when the destination file uses CRLF, since the shipped block is LF. The options are writing the block in the file's style, or keeping the exact shipped bytes and accepting mixed endings. State your choice, justify it against item 1's "exactly this text", and test it. Whatever you choose, a second run on the result must change nothing.
6. Guarded writes, per spec §27 and consistent with T33a. Reject a symlinked `AGENTS.md` or `CLAUDE.md`, and any destination outside the root, before writing anything, leaving the tree unchanged. Preserve the file's permissions when updating it. On this machine the filesystem is case-insensitive, so `agents.md` and `AGENTS.md` name the same file; decide how selection behaves when a file exists under a different case, and state it.
7. `init --json` reports the destination in `snippet_file` as a root-relative path, or `null` when no snippet was written. The destination appears in `created` when newly created and in `modified` when updated. It appears in neither when unchanged, so a repeated `init --write-snippet` returns empty lists.
8. Once `snippet` is implemented, no command remains unimplemented. `crates/rivet-cli/tests/index_json.rs::unimplemented_command_emits_json_error` currently targets `snippet` and will break. Remove the not-implemented path from `main.rs` rather than leaving dead code, and replace that test with one asserting that every subcommand is wired, for example that each subcommand's `--help` exits 0. Say what you replaced it with.
9. Human output is T34. Without `--json`, `init --write-snippet` adds one plain line naming the snippet destination to T33a's summary.
10. Tests, in `crates/rivet-cli/tests/init_json.rs` or a new `snippet_json.rs`, driving the real binary and asserting exact file bytes. Cover: the doc-equality test from item 1; `snippet` and `snippet --json` outside a repository; each auto-selection case including both files present giving exit 2; an explicit `--snippet-file` overriding auto-selection and creating a missing file; a rejected name; append into an existing file with content before it; replacement of an outdated block with the text before and after it preserved; the idempotent repeat changing no byte and reporting empty lists; each malformed-block form rejected with the tree unchanged; the CRLF case; a symlinked destination rejected; and preserved permissions.
11. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts.
12. Update docs/TASKS.md: tick T33b, leaving the T33 parent unticked since T33c remains and is gated on an agreed live-host budget. Set Last completed = T33b and Next = T34, since TASKS.md says to perform T33c after T34 and before the pilot. Add a one-line "Last checks". Do not touch other docs. Do not run git.

Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
