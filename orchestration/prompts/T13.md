IMPORTANT: you are working in a git worktree on branch `task/T13`, in parallel with T14 (signatures, `symbol --source`, a new fixture file) and T17 (PHP use extraction). To keep merges clean: put all query-form logic in `crates/rivet-index/src/` (new file `query.rs` if `resolve_query` is not already in its own module) and tests in a NEW file `crates/rivet-cli/tests/symbol_query_forms.rs`. Touch `crates/rivet-cli/src/` only where the argument validation for `--limit`/`--offset` lives, and do not reorganize existing files. Do not edit `crates/rivet-languages/` except to add `php::lookup_name` case rules if missing. In `docs/TASKS.md` tick T13 only and add a note on the "Active task / partial progress" line; do not change "Last completed"/"Next" (the orchestrator reconciles on merge). Do not run git.

Implement T13 from docs/TASKS.md only. Read AGENTS.md first, then the T13 row, spec §10.2 (File and line row) and §10.3, and docs/OUTPUT-CONTRACT.md "Errors" (ambiguous_symbol, symbol_not_found) and "Ordering and versioning" (Symbol candidates and Suggestions rows). Nothing else.

T13 — `file:line` queries, query normalization hardening, deterministic ambiguity pagination, and not-found suggestions. T12 already implemented basic forms, pagination, and Levenshtein suggestions; this task completes and hardens them.

Requirements:
1. `file:line` form in `rivet-index::resolve_query`: recognized when the query contains a `:` and the text after the LAST colon parses as a positive integer, and the text before it is a repository-relative path that exists in the `files` table (use `/` separators; reject absolute paths and `..` components with exit 2). Select the innermost symbol(s) whose `start_line <= line <= end_line` in that file; "innermost" means smallest `(end_byte - start_byte)`. If several innermost symbols share the same minimal span length on that line (e.g. two one-line declarations on one line), return ambiguity (exit 5) with those candidates; never guess. If the file is indexed but no symbol encloses the line → exit 4 with suggestions being the nearest enclosing or adjacent symbols in that file (up to 5, by distance in lines). If the path is not in the index → exit 4 with a hint naming the path and saying it is not indexed or is excluded. A PHP qualified name never contains `:` followed by digits at the end, but guard anyway: try canonical-ID and native forms BEFORE file:line so `Foo::bar` is never misread.
2. Normalization hardening for the dotted form: normalize `::`, `->`, `\`, `/`, and `.` to `.`; collapse runs of separators; strip a single leading separator; matching is at component boundaries only (`Service.launch` must not match `SurveyService.launch`). Short names use `lookup_name` with the language's case rule (PHP classes/functions/methods case-insensitive, properties/constants case-sensitive; a property query must include the `$`).
3. Deterministic ambiguity pagination: candidates sorted by `(file bytes, start_byte, id)` before slicing; `total`, `truncated`, `next_offset` per the contract; `--offset` beyond the end yields an empty `candidates` page with the correct `total` and `next_offset: null`. `--limit` outside 1..=1000 or negative `--offset` → exit 2 before any filesystem work (validate arguments first).
4. Suggestions: up to 5 distinct `qualified_name`s ordered by `(Levenshtein distance over Unicode scalars between the query's last component and the symbol's `name`, then qualified_name bytes)`; case-insensitive distance for PHP insensitive kinds. Keep the implementation in `rivet-index`.
5. Integration tests in `crates/rivet-cli/tests/symbol_query_forms.rs` on a temp copy of the authored fixture: `SurveyService.php:19` (inside `launch` body) resolves to `App\Services\SurveyService::launch`; `SurveyService.php:9` (class line) resolves to the class; `SurveyService.php:2` (blank line, no symbol) exits 4 with suggestions naming the class; `nope.php:3` exits 4 with the not-indexed hint; `boot.php:8` resolves to `App\Boot\launch`; `Service.launch` exits 4 (no component-boundary match); `services.surveyservice.LAUNCH` resolves (case-insensitive method); `launch --limit 1 --offset 1` returns one candidate, `next_offset == 2`; `launch --offset 99` returns empty candidates with `total == 3`; `launch --limit 0` exits 2; `App\Services\SurveyService::$Label` (wrong case on a property) exits 4.
6. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`. Paste real results.
7. Update docs/TASKS.md: tick T13, Last completed = T13, Next = T14, one-line "Last checks". Do not touch other docs. Do not run git.

Finish with the AGENTS.md final block.
