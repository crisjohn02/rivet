Implement T32 from docs/TASKS.md only. Read AGENTS.md first, then the T32 row; spec §27 (Security and Failure Boundaries) in full, and §10 on query forms; `docs/ARCHITECTURE.md` "Parse and coverage policy"; `docs/OUTPUT-CONTRACT.md` "Errors" (the table and the paragraph beneath it) and "Common index metadata"; `crates/rivet-index/src/query.rs` (`QueryOutcome`); `crates/rivet-cli/src/symbol.rs`, `refs.rs`, `context_cmd.rs`, `refresh.rs`, `transport.rs`; `crates/rivet-parser/src/lib.rs`; and the PHP walkers in `crates/rivet-languages/src/php/`. Nothing else.

`docs/OUTPUT-CONTRACT.md` is normative for errors. Where this prompt and the documents disagree, they win and you report the disagreement.

T32 — Complete parser resource limits, skip and diagnostic handling, and the direct-target errors.

What the documents require, and the current state:

1. Parser resource bounds do not exist. Spec §27: "Parser work must have cooperative cancellation/resource bounds, initially one million visited syntax nodes and 500,000 extracted uses per file, plus the file-size limit." `ParseStatus::ResourceLimit` and the `resource_limit` coverage count exist, but nothing ever produces them. Count visited syntax nodes and extracted uses per file while extracting; a file exceeding either bound is recorded as `resource_limit`, contributes no symbols, uses or scopes, and gets a diagnostic. The limits are counts, never wall-clock time: "Deterministic limits avoid wall-clock-dependent query results." Make both limits constants with the spec's values, and make them lowerable in tests so the tests do not need million-node files.
2. A file that fails must lose its previous facts. Spec §27: "A syntax-error or parser-resource-limit file contributes no symbols/uses in v0.1; remove its previous facts and report diagnostics." ARCHITECTURE: "Clear formerly valid facts when a file becomes invalid; unrelated query results remain available with partial coverage." Check whether this already holds for parse errors, and make it hold for both. Also: "Tree-sitter recovery must not cause old facts to be mislabeled as current."
3. Direct-target errors are wrong today. When a query addresses a file that is not indexed, `QueryOutcome::PathNotIndexed` becomes `symbol_not_found`, exit 4, whatever the reason. The contract assigns the code by WHY the file is not indexed:
   - an unsupported file is `unsupported_language`, exit 7, with `file` and a nullable `language`;
   - a parse-error or resource-limit file is `parse_failure`, exit 6, with `file` and `detail`;
   - a binary, oversize or encoding exclusion is `repository_unavailable`, exit 3, "with the reason and a corrective hint";
   - "A missing name elsewhere is exit 4 even when coverage is partial."
   This applies to every direct form, meaning a `file:line` query and a canonical ID whose file part names such a file. Spec §27: "Targeting that file by path/ID returns exit 6." It applies in `symbol`, `refs` and `context` alike. A non-PHP file with a TypeScript extension is unsupported with `language: "typescript"`; a file of no known language has `language: null`.
4. An invalid `file:line` is rejected after the refresh. The contract says "Invalid arguments take precedence over filesystem work." Today `QueryOutcome::InvalidFileLine` is produced by query resolution, which runs after refresh. Split the syntactic validity of a `file:line` query, meaning its form, a line of zero, an absolute path, or a parent-directory path, from checks that genuinely need the snapshot, such as a line beyond the end of the file. Move the syntactic checks before any filesystem work in all three commands. Say which checks remain after refresh and why each one needs the snapshot.
5. I/O errors already fail refresh with exit 3 in `refresh.rs`. Verify that an unreadable file fails refresh rather than silently preserving old facts, and add a test if none exists. Note that on this machine a file made unreadable with `chmod 000` may still be readable if tests run as root; they do not here, but guard the test accordingly.

Requirements:

1. Every error must carry exactly the contract's required fields, and `index` where the error is index-dependent per AF5's `is_index_dependent`. A direct-target error arises only after a snapshot is acquired, so decide whether each new error is index-dependent, and state it.
2. Unrelated queries keep working with partial coverage: with one file failing, a query for a symbol in a different file succeeds and reports `complete: false` with the failing file's diagnostic.
3. Do not change any successful result. The authored fixture's `index --json`, and `symbol`, `refs` and `context` output for its symbols, must be byte-identical to before, digest included, since no fixture file fails. Gold must verify 51 entries.
4. Bump `fact-schema` only if recorded facts change shape or meaning; state whether you did.
5. Tests, driving the real binary, asserting exact exit codes and JSON fields. Cover:
   a. A file exceeding the lowered node limit and one exceeding the lowered use limit: each recorded `resource_limit` with no facts and a diagnostic, and the same file under the default limits indexed normally, proving the limits are counts that bite only when exceeded.
   b. A formerly valid file that becomes a parse error, then a resource-limit file: its previous symbols, uses and bindings are gone, and `refs` for a symbol it used to declare no longer lists its uses.
   c. Each direct-target code, for `file:line` and for a canonical ID, in each of `symbol`, `refs` and `context`: exit 7 with `language` for an unsupported and a TypeScript file; exit 6 with `detail` for a parse error and a resource limit; exit 3 with a reason and hint for a binary, an oversize and, if constructible here, an encoding exclusion; and exit 4 for a missing name in an indexed file while coverage is partial.
   d. Each invalid `file:line` form rejected with exit 2 in a directory that is not a repository, proving it happens before filesystem work.
   e. An unreadable file failing refresh with exit 3.
   f. An unrelated query succeeding with `complete: false` and the diagnostic present.
6. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts.
7. Update docs/TASKS.md: tick T32, Last completed = T32, Next = T33, one-line "Last checks". Do not touch other docs. Do not run git.

If this grows beyond one coherent change, split it as T32a, the resource limits and stale-fact removal, and T32b, the direct-target errors and argument precedence. Record what works and what remains, and do not tick T32. Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
