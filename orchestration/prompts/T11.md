Implement T11 from docs/TASKS.md only. Read AGENTS.md first, then the T11 row and the "Read:" line for section C, docs/ADDING-A-LANGUAGE.md "MVP support boundary" (PHP column, rows Named definitions and Qualified names), "Adapter contract", and "Implementation checklist"; docs/ARCHITECTURE.md "Parse and coverage policy"; tests/fixtures/php/authored/README.md and tests/gold/php-authored.toml. Nothing else.

T11 — PHP named-definition extraction into owned records, with parse-error detection. No database, no uses/references, no CLI wiring.

Requirements:
1. In `rivet-core`, add the owned extraction record types the adapter contract needs for THIS task only: `ExtractedSymbol { qualified_name: String, name: String, kind: SymbolKind, span: Span, parent_index: Option<usize> (index into the same Vec), signature: Option<String>, doc_comment: Option<String> }`, `ExtractedFile { symbols: Vec<ExtractedSymbol>, diagnostics: Vec<Diagnostic> }`, `Diagnostic { code: String, detail: String, start_byte: Option<u32> }`. Leave `signature`/`doc_comment` as `None` in T11 (T14 fills them). No `Node` handles or lifetimes in these types.
2. In `rivet-languages` behind `lang-php`, add `php` module with `pub fn extract(source: &[u8], tree: &tree_sitter::Tree) -> ExtractedFile`. Add `rivet-core` as a dependency of `rivet-languages`. Extract: `namespace_definition` → `Module` with qualified name = the namespace name; `class_declaration` → `Class`; `interface_declaration` → `Interface`; `trait_declaration` → `Class` (traits are class-kind in v0.1 per ADDING-A-LANGUAGE); `enum_declaration` → `Enum`; `function_definition` → `Function`; `method_declaration` → `Method`; `property_declaration` → one `Property` per declared property, name retaining `$`; `const_declaration` inside a class → one `Const` per constant. Qualified names use the PHP native form: namespace joined with `\`, members with `::` (`App\Services\SurveyService::launch`, `App\Services\SurveyService::$label`, `App\Boot\launch`). A file with no namespace uses the bare name. Spans are the whole declaration node including modifiers (Tree-sitter's node range). `parent_index` points at the enclosing class/interface/trait/enum for members; namespaces are NOT parents of classes (a Module record exists but classes keep `parent_index = None`), matching the spec's parent field meaning "parent class or module" only for members.
3. Parse policy: if `tree.root_node().has_error()` is true, return `ExtractedFile { symbols: vec![], diagnostics: [Diagnostic { code: "parse_error", detail: "<first ERROR or MISSING node kind> at byte N", start_byte: Some(N) }] }`. Find the first error/missing node by a depth-first walk using `TreeCursor`, not by regex. Also walk-and-count nodes with a hard cap of 1,000,000 visited nodes; on exceeding it, return `resource_limit` diagnostic and no symbols.
4. Prefer a Tree-sitter `.scm` query file at `crates/rivet-languages/src/php/symbols.scm` loaded with `include_str!` and compiled once via `std::sync::OnceLock`; use captures `@symbol.<kind>` and `@symbol.name`. If a construct cannot be captured by query cleanly (nested property lists), a documented small cursor walk is acceptable. Validate node names against the pinned grammar by running the tests; do not guess.
5. Tests in `crates/rivet-languages/tests/php_extract.rs`: parse each of the three fixture files (read from `tests/fixtures/php/authored/` relative to `CARGO_MANIFEST_DIR/../..`) and compare against `tests/gold/php-authored.toml` `[[declaration]]` entries: for every gold declaration there must be exactly one extracted symbol with the same `qualified_name`, `kind`, `start_byte`, and `end_byte`; report any extra extracted symbols that have no gold entry EXCEPT `Module` records (gold has none) and fail if any. Parse the TOML with a tiny hand-rolled reader or add `toml` as a dev-dependency (already pinned in the workspace). Also test: the malformed PHP snippet from grammar_smoke yields zero symbols and one `parse_error` diagnostic with a byte offset; a file with no namespace yields bare names; parent_index of `launch` points at its class.
5b. Guard test: in `crates/rivet-languages`, add a unit test that reads `Cargo.lock` from `CARGO_MANIFEST_DIR/../..`, finds the `version` of `tree-sitter-php` (and `tree-sitter-typescript` when that feature is enabled), and asserts each version string appears in `EXTRACTOR_FINGERPRINT`. This stops the hand-maintained fingerprint from silently drifting when a grammar is bumped.

6. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test -p rivet-languages`, and `cargo build -p rivet-cli --no-default-features` (must still compile with the php module gated off). Paste real results.
7. Update docs/TASKS.md: tick T11, Last completed = T11, Next = T12, one-line "Last checks". If a gold entry turned out to disagree with Tree-sitter's actual node span, do NOT edit the gold silently: report the exact discrepancy under "Contradictions found in docs" and leave the test failing. Do not run git.

Finish with the AGENTS.md final block.
