Implement T24 from docs/TASKS.md only. Read AGENTS.md first, then the T24 row; `docs/OUTPUT-CONTRACT.md` sections "Pagination and resolution", "`rivet symbol <query> --json`", "Coordinates and symbol objects", "Flag applicability", and "Ordering and versioning"; spec §14 and §20.4; and the existing `crates/rivet-cli/src/symbol.rs` and `crates/rivet-cli/src/refs.rs` (T23). Nothing else.

`docs/OUTPUT-CONTRACT.md` is normative. Where this prompt and the contract disagree, the contract wins and you report the disagreement.

T24 — Populate the `symbol` command's `calls` and `called_by` lists with the same reference records `refs` produces, paginated independently.

Requirements:

1. Reuse T23's machinery. `refs.rs` already builds reference records, applies reference-mode matching, filters by minimum resolution, sorts by the contract order, dedupes, counts, and slices. Extract what you need into a shared helper rather than writing a second implementation; two independent copies will drift. State exactly what you extracted and confirm `refs` output is unchanged by re-running its tests. Do not restructure either command beyond that extraction.

2. Semantics, from the contract:
   - `calls` selects use sites CONTAINED BY the target symbol, that is uses whose `containing_symbol` is the target.
   - `called_by` applies default reference-mode matching to call sites targeting the symbol, that is the same rule `refs --mode references` uses, restricted to `ref_kind: call`.
   - Each item is a full reference object with the same eleven keys in the same order as `refs`, with `ref_kind: call`.
   - Count CALL SITES, not unique functions. A function called three times from one caller contributes three items and counts three.
   - Top-level calls are retained. A use with no containing symbol has `containing_symbol: null` and must not be dropped from `called_by`.

3. Each list is an object with exactly `total`, `truncated`, `next_offset`, `items`, in that order, and the two lists paginate INDEPENDENTLY using the same supplied `--limit` and `--offset`. Independent means each list computes its own `total`, `truncated`, and `next_offset` against its own match set; a short `calls` list must not affect `called_by`'s paging. `--min-resolution` applies to both lists.

4. `--signature-only` omits both lists entirely and conflicts with `--source`. Both behaviors already exist; keep them and assert them.

5. Never invent a tier. An item's `resolution` is the query-relative value, computed exactly as `refs` computes it. `name_match` is never persisted.

6. Tests, extending `crates/rivet-cli/tests/symbol_json.rs` or a new file (say which). Assert exact spans and target IDs, not just counts. Cover, each as its own test:
   a. A method with known callees: `calls` items match the gold spans of the uses inside it.
   b. A method called from several places: `called_by` contains each call site with correct containing symbols.
   c. Repeated call sites: a target called more than once from a single caller yields one item per call site and a `total` counting call sites, not callers.
   d. A top-level call is retained in `called_by` with `containing_symbol: null`.
   e. Independent pagination: a limit smaller than both lists pages each correctly and separately; assert one list's `next_offset` while the other's is null, using a symbol where the two lists differ in length.
   f. `--min-resolution scoped` filters both lists and both totals.
   g. `--signature-only` omits both keys entirely; with `--source` it is `invalid_arguments` exit 2.
   h. Determinism: the same query twice is byte-identical.

7. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts.

8. Update docs/TASKS.md: tick T24, Last completed = T24, Next = T25, one-line "Last checks". A row for T21a may be present and unticked; leave it alone, another session owns it. Do not touch other docs. Do not run git.

Report contradictions rather than silently resolving them. If this grows beyond one coherent change, split as T24a/T24b and do not tick T24.

Finish with the AGENTS.md final block.
