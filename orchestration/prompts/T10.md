Implement T10 from docs/TASKS.md only. Read AGENTS.md first, then the T10 row, docs/OUTPUT-CONTRACT.md sections "Transport and common rules", "Flag applicability" (index row only), "Common index metadata", the `index --json` block under "Administrative commands", and "Errors"; spec §12.4 steps 1–2 and §13. Nothing else.

T10 — Wire the first real `rivet index --json` path end to end, plus the shared JSON success/error transport. This is an inventory milestone: no parsing, no symbols. All files are recorded with `parse_status` `unsupported` except that files whose extension maps to an enabled language (`.php` for lang-php, `.ts`/`.tsx` for lang-typescript) are read via T07 and stored with their bytes and `parse_status = "ok"` IF they read successfully (they are not parsed yet; that is T11+), otherwise the T07 skip reason maps to `binary`, `size`, `encoding`; I/O failure aborts the refresh with exit 3.

Requirements:
1. Add `serde` and `serde_json` (pinned `=x.y.z`; serde is already pinned by T05, reuse it) to `rivet-cli`. Implement a small transport module in `rivet-cli`: `emit_success(value: serde_json::Value)` writes exactly one JSON object with no insignificant whitespace followed by `\n` to stdout; `emit_error(code, exit, message, hint, extra)` writes one JSON error object followed by `\n` to stderr, leaves stdout empty, and exits with the documented code. Every object gets `"schema_version": 1` as its FIRST key. Use `serde_json::Map` with insertion order preserved (enable the `preserve_order` feature) so key order is exactly as documented.
2. `rivet index [--json] [--force] [--timing] [--languages a,b] [--freshness content|metadata]`: discover root (T05; failure → exit 3 `repository_unavailable`), load config (invalid → exit 2 `invalid_arguments` with the key name in the message), create `.rivet/` at a Git root if missing (spec §25: auto-create only at a Git root, never touch `.gitignore`), open the store (T08), walk (T06), read eligible language files (T07), build the inventory, publish (T09) passing `Fingerprint { index_format_version: rivet_store::INDEX_FORMAT_VERSION.to_string(), effective_config: config.fingerprint(), extractor: "php=<grammar crate version>;ts=<grammar crate version>" derived from `env!("CARGO_PKG_VERSION")`-style constants you define in rivet-languages (a const string listing enabled grammars and their pinned versions), resolver: "none" }` (never a literal version string), and emit the `index --json` shape exactly: `schema_version`, `index` (the common index metadata: `snapshot`, `freshness`, `coverage {complete, files_seen, files_indexed, skipped {unsupported, binary, size, encoding, parse_error, resource_limit}}`, `diagnostics {total, truncated, items[]}`), `symbols` (0 for now), `uses` (0), `bindings` (0), `updated`, `unchanged`, `deleted`, and `elapsed_ms` only when `--timing`. `files_indexed` counts `parse_status = ok`; `files_seen = files_indexed + sum(skipped)`; `complete` is true only when all skip counts are zero. Diagnostics: one item per binary/size/encoding skip and per non-UTF-8 path from the walk, sorted by `(file bytes, code)`, capped at 50 with `truncated` set. `--languages` not compiled into the binary → exit 2. `--freshness metadata` is accepted and recorded in `index.freshness` but in T10 both modes hash content (metadata mode is T16); say so in a code comment.
3. Human (non-JSON) output for `index`: the spec §13 shape (Indexed N files / symbols / relationships / Updated / Unchanged / Elapsed). Progress text is allowed only in human mode.
4. The other five subcommands keep their not-implemented behavior, but in `--json` mode they must emit a JSON error object (`error: "general"`, exit 1, message naming the task) instead of plain text. Add `--json` as a global flag on the CLI for this.
5. Integration tests in `crates/rivet-cli/tests/index_json.rs` using `std::process::Command` on the built binary (`env!("CARGO_BIN_EXE_rivet")`): create a temp repo with a `.git` dir, two `.php` files, one `.md`, one binary `.php` (NUL byte), one oversize `.php` (set `max_file_size_kb = 1` in config); assert stdout parses as JSON, `schema_version` is the first key, `coverage.files_seen == 5`, `files_indexed == 2`, `skipped.unsupported == 1`, `skipped.binary == 1`, `skipped.size == 1`, `complete == false`, `diagnostics.total == 2`, stderr is empty; run again and assert `updated == 0`, `unchanged == 5`, and the same `snapshot`; run with `--timing` and assert `elapsed_ms` present and absent otherwise; run `rivet symbol Foo --json` and assert stderr is a JSON object with `error == "general"`, stdout empty, exit 1; run in a temp dir with no `.git` and no `.rivet` and assert exit 3 with `error == "repository_unavailable"`.
6. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`. Paste real results.
7. Update docs/TASKS.md: tick T10, Last completed = T10, Next = T11, one-line "Last checks". Do not touch other docs. Do not run git.

Finish with the AGENTS.md final block.
