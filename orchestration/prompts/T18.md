Implement T18 from docs/TASKS.md only. Read AGENTS.md first, then the T18 row, docs/ARCHITECTURE.md "Minimal logical schema" (`uses`, `scopes`, `bindings` tables and the paragraph after the schema) and "Resolution and context" (first paragraph), spec §12.2–§12.3, and the T17 types in `crates/rivet-core/src/extract.rs` (`ExtractedUse`, `UseHint`, `ExtractedImport`). Nothing else.

T18 — Persist lexical scopes, imports/aliases, and unresolved use facts. No resolution (bindings stay empty), no CLI output changes.

Requirements:
1. `rivet-store`: add `UseRow` mirroring the `uses` table (use_id assigned by SQLite, file, containing_symbol: Option<String> (symbol id), scope_key, spelling, lookup_name, ref_kind, start_byte, end_byte, line, col, receiver: Option<String>, hint_json: String) and `ScopeRow` mirroring `scopes` (file, scope_key, parent_scope_key: Option<String>, facts_json: String). Extend `InventoryInput`/`publish_inventory` so each file's uses and scopes are replaced together with its symbols inside the SAME single transaction (delete by file, insert). Add `list_uses_for_file(path)`, `find_uses_by_lookup_name(name)` ordered by `(file bytes, start_byte, end_byte, ref_kind)`, and `list_scopes_for_file(path)`.
2. `hint_json` is the serde_json serialization of `UseHint` (add `serde` derive to the T17 types in rivet-core behind the existing serde dependency; use `#[serde(tag = "kind", rename_all = "snake_case")]`). `facts_json` for a scope holds: `imports: [{alias, target_qualified, kind, span}]` visible in that scope, `typed_bindings: [{variable, type_spelling, span}]` (parameters/promoted properties/typed locals), `new_bindings: [{variable, class_spelling, span}]`, and `declares: [symbol ids declared directly in this scope]`. Language adapters produce these facts as owned data (extend `ExtractedFile` with `scopes: Vec<ExtractedScope { scope_key, parent_scope_key, facts }>` in rivet-core, and have the PHP adapter populate them from what T17 already computes). The point (ARCHITECTURE): enough persisted evidence to re-resolve every use WITHOUT reparsing the file.
3. `lookup_name` for a use follows the same PHP case rule as symbols (`php::lookup_name(spelling, kind-of-target-being-referenced)`): for `Call`/`Type`/`Import` lowercase; for `Read`/`Write` on `$prop`/`CONST` preserve case. Where the referenced kind is unknown (`Unknown`), store lowercase and note it in a comment.
4. Indexing: wire T17's `extract` output through `rivet_parser::parse_file`'s `ExtractedFile` (if T15 has not yet moved parsing into rivet-parser, wire through the existing call site and leave a TODO naming T15) into `UseRow`/`ScopeRow` and include them in the publish. `containing_symbol` is the canonical id of the symbol at `containing_symbol_index`, or NULL for top-level uses; a NULL is a real value and must be persisted (the whole point of nullable containers). `index --json` `uses` count becomes real; `bindings` stays 0.
5. Tests: store unit tests for round-tripping `UseRow` with NULL container and a non-ASCII receiver, byte ordering of `find_uses_by_lookup_name`, and cascade delete of uses/scopes when a file row is deleted. Integration test `crates/rivet-cli/tests/index_uses.rs`: index the authored fixture and assert `uses` equals the number of gold `[[use]]` entries plus the count of extra non-launch uses you can enumerate deterministically (or assert `>=` gold count and that every gold `(file,start_byte,end_byte,ref_kind)` exists via a store query in a test helper binary — prefer opening the store directly from the test using `rivet_store::Store::open` on the temp `.rivet/`); assert the top-level `launch();` row has `containing_symbol IS NULL`; assert re-indexing with no edits yields identical use rows (compare full row lists); assert `scopes` for `ReportService.php` include the alias import `SurveySvc → App\Services\SurveyService`.
6. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`. Paste real results.
7. Update docs/TASKS.md: tick T18, Last completed = T18, Next = T19, one-line "Last checks". Do not touch other docs. Do not run git.

Finish with the AGENTS.md final block.
