Implement T33a from docs/TASKS.md only. Read AGENTS.md first, then the T33 and T33a rows and the paragraph beneath the T33 subtask table; spec §22 (Configuration), §25 (Repository Discovery) and §27 (Security and Failure Boundaries); `docs/OUTPUT-CONTRACT.md` "Administrative commands" (the `init --json` shape), "Flag applicability" and "Errors"; `docs/INTEGRATION.md` for the user workflow; `crates/rivet-core/src/config.rs` and `root.rs`; and `crates/rivet-cli/src/main.rs`. Nothing else.

`docs/OUTPUT-CONTRACT.md` is normative for JSON and errors. Where this prompt and the documents disagree, they win and you report the disagreement.

T33a — Implement core `rivet init`: create `.rivet/` and its config, add `.rivet/` to the root `.gitignore` when needed, and guard every write. The snippet and instruction-file installation, `--write-snippet` and `--snippet-file`, are T33b; do not build them. If you add those flags now for argument parsing, they must fail clearly naming T33b, never succeed with a placeholder.

Requirements:

1. Root selection follows spec §25: the nearest ancestor containing `.rivet/` or a `.git` file or directory, stopping at the first boundary. Outside Git, `init` establishes the CURRENT directory as the root. Reuse the existing discovery in `root.rs` rather than writing a second one.
2. `init` creates `.rivet/` and `.rivet/config.toml`, and appends `.rivet/` to the root `.gitignore` only when it is not already ignored there. It creates `.gitignore` only if none exists; say whether you create one outside Git, and why.
3. The default config file. Spec §22 gives its contents. The trap: the file `init` writes must mean exactly what having NO config file means, or merely running `init` changes the effective configuration fingerprint and silently forces a reindex with different settings. Compare the spec's template against the built-in defaults in `config.rs`. The spec's `exclude` list, for example, may differ from the built-in default exclusions in §26, and §26 says configured exclusions are additional. Write a file whose effective configuration, as the loader computes it, is identical to the defaults, and prove it with a test comparing the effective-config fingerprint with and without the file. If the spec's template cannot be both literal and equivalent, report the contradiction and say which you chose.
4. Idempotence, per spec §25 and the contract. Repeated `init` preserves the existing configuration byte for byte, never duplicates the `.gitignore` entry, and may return empty `created` and `modified` lists. A user who edited `config.toml` keeps their edits. An existing `.gitignore` keeps all its other content and line endings.
5. `init --json` returns exactly the contract's shape and key order: `schema_version`, `created`, `modified`, `snippet_file`. Paths are root-relative and sorted by UTF-8 bytes, a directory is written with a trailing slash as `.rivet/` is in the contract, and `snippet_file` is `null` in T33a.
6. Guarded writes, per spec §27. Reject a symlinked `.rivet`, a symlinked `config.toml`, and a symlinked `.gitignore`, rather than writing through them. Every written path must stay within the selected root. Preserve the existing permissions of `.gitignore` when appending to it. Decide the error code for each rejection from the contract, most likely `repository_unavailable` with exit 3 or `invalid_arguments` with exit 2, and justify each. Validate every destination BEFORE writing anything, so a rejected run leaves the tree exactly as it was.
7. Remove the not-implemented placeholder for `init` and update the not-implemented hint. `crates/rivet-cli/tests/index_json.rs::unimplemented_command_emits_json_error` currently targets `snippet`, which stays unimplemented until T33b, so it needs no retargeting; confirm it still passes.
8. Human output is T34. Without `--json`, print a short plain summary of what was created or modified, without building new formatting.
9. Tests, in a new `crates/rivet-cli/tests/init_json.rs`, driving the real binary and asserting exact JSON and exact file contents. Cover: first run in a Git repository; first run outside Git establishing the current directory; a nested directory inside a repository resolving to the repository root; repeat runs returning empty lists and changing no byte of the config or `.gitignore`; a `.gitignore` that already ignores `.rivet/`, including under an equivalent spelling you decide is equivalent and justify; an edited config preserved; each symlink rejection leaving the tree unchanged; `.gitignore` permissions preserved; the effective-config equivalence from item 3; and a following `rivet index` giving the same digest as indexing without having run `init`.
10. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts.
11. Update docs/TASKS.md: tick T33a only, leaving the T33 parent unticked since T33b and T33c remain, and set Last completed = T33a, Next = T33b, with a one-line "Last checks". Do not touch other docs. Do not run git.

Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
