Implement T28 from docs/TASKS.md only. Read AGENTS.md first, then the T28 row; spec §16.2 (Collapsed Form) and §16.4 items 1 and 2 plus its final paragraph; `docs/OUTPUT-CONTRACT.md` the `rivet context` section (the estimate rule, `form`, and error 8 `budget_too_small` in the Errors table); `crates/rivet-cli/src/context.rs` (T26/T27); `crates/rivet-cli/src/symbol.rs` (its stored-source slicing); `crates/rivet-cli/src/transport.rs`; `crates/rivet-languages/src/lib.rs` and `src/php/signature.rs`. Nothing else.

The spec is normative for fitting and the contract for the estimate rule and error shape. Where this prompt disagrees with either, they win and you report the disagreement.

T28 — Source estimation and greedy budget fitting for all three collapse modes. Overlap suppression, `--limit`, and the omission counts are T29; command wiring is T30. Do not build them, and leave `main.rs` routing `context` to `not_implemented` naming T30.

Requirements:

1. The estimate is exactly `ceil(UTF8_bytes / 3)` of each final source string, summed across segments. Integer arithmetic only, never floating point. The ellipsis in a collapsed body is three UTF-8 bytes and counts as such. A successful result must satisfy `estimated_tokens <= budget_tokens`, and a segment that fits exactly at the budget fits.

2. Two forms per candidate. `full` is the symbol's stored source slice for its declaration span, taken from the snapshot's stored bytes, never from the current file on disk. `signature` is the collapsed form of spec §16.2: declaration line or lines, doc comment, and for a container its member signatures with bodies replaced.

3. Build the signature form through `signature_summary`, which already exists and was corrected in T25a. It does not need a parse tree; it reads the declaration span, the stored signature, and the stored source text. Bridge stored rows to its input. Do NOT call the PHP module directly from `rivet-cli`: add a language-dispatch function in `crates/rivet-languages/src/lib.rs` keyed by language, as `grammar` already is, and call that. Say exactly what you added. Note in your report that `refresh.rs` already reaches `rivet_languages::php::lookup_name` directly; report it, do not fix it.

4. Check whether `signature_summary` includes the doc comment. Spec §16.2 says the signature form does. If it does not, the context signature form must include it, and you must say where you added it.

5. Fitting, per spec §16.4:
   a. The target always tries its full body first, in every mode; the spec says "`--collapse always` still prefers the target in full if it fits". If full does not fit, `auto` and `always` try the signature, while `never` fails.
   b. Every other candidate, in rank order: `auto` tries full then signature; `always` tries signature only; `never` tries full only.
   c. A candidate with no allowed form that fits is skipped, and fitting CONTINUES to later candidates. A smaller later candidate may still fit. Do not stop at the first miss.
   d. No partial bodies, ever. No truncation of a source string to squeeze it in.

6. Error 8. If no allowed target form fits, return `budget_too_small` with exit code 8 and the contract's required fields `budget_tokens` and `required_tokens`, where `required_tokens` is the minimum estimate among the target's ALLOWED forms, so under `never` it is the full estimate. Add a `CliError` constructor for it in `transport.rs` matching the existing ones.

7. Tests. Assert exact estimates and exact emitted forms, not merely that something fit. Cover: the estimate's rounding at 1, 3 and 4 bytes, and a string with multibyte characters; a target fitting exactly at the budget, and one token under failing over to its signature; each of the three modes on the same candidate set with the differing results asserted; `always` still emitting the target in full when it fits; a skipped large candidate followed by a smaller one that fits; `never` returning error 8 with `required_tokens` equal to the full estimate, and `auto` with the signature estimate; the total never exceeding the budget across a sweep of budgets, which is the property this task exists to guarantee; and determinism.

8. The fitted result is an intermediate structure for T29 and T30, not JSON. Carry for each segment the candidate, its form, its source string and its estimate, plus the total.

9. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts.

10. Update docs/TASKS.md: tick T28, Last completed = T28, Next = T29, one-line "Last checks". Do not touch other docs. Do not run git.

If this grows beyond one coherent change, split as T28a/T28b, record what works and what remains, and do not tick T28. Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
