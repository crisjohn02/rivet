Implement T04 from docs/TASKS.md only. Read AGENTS.md first, then the T04 row, spec §11.3–§11.5, and docs/ADDING-A-LANGUAGE.md sections "MVP support boundary" (PHP column) and "Fixtures and gold data". Nothing else.

T04 — Authored three-file PHP fixture with exact gold spans. No Rust code changes.

Requirements:
1. Create `tests/fixtures/php/authored/` with exactly three PHP files (LF line endings, UTF-8, `<?php` opening tag, `declare(strict_types=1);`, namespaces). Together they must contain, each clearly commented in the source with `// gold:` markers naming the case:
   a. Two classes in different namespaces each defining a method with the same name (`launch`), so the short name is ambiguous.
   b. A `use ... as` alias import of one of those classes, and a call through the alias, so an alias use has a different spelling than the target.
   c. A top-level function call outside any class or function (a use with no containing symbol).
   d. A `$this->launch()` call inside one of the classes (scoped via this).
   e. A local `$svc = new \App\Services\SurveyService(); $svc->launch();` (scoped via new).
   f. A method with a typed parameter `SurveyService $svc` calling `$svc->launch()` (scoped via explicit type).
   g. An untyped receiver `$x->launch()` where `$x` comes from an unknown source (must remain name_match).
   h. The word `launch` inside a comment and inside a string literal (must NOT be a use), and a string interpolation `"{$svc->launch()}"` (its inner expression IS a use).
   i. A named function and a class constant and a typed property, so later tasks have those kinds available.
   Keep each file under 60 lines. Keep line lengths under 100 columns.
2. Create `tests/gold/php-authored.toml` OUTSIDE the fixture directory with two arrays of tables:
   `[[declaration]]`: file, qualified_name (PHP native, e.g. `App\Services\SurveyService::launch`), kind, start_byte, end_byte, start_line, end_line, and `text` (the first 40 bytes of the span, for human verification).
   `[[use]]`: file, start_byte, end_byte, line, column, ref_kind, spelling, `expected_target` (a gold declaration qualified_name or "" if unresolved by design), `expected_resolution` (exact | scoped | name_match), `case` (the letter above), and `text`.
   Include at least: every `launch` declaration, every `launch` use from cases b–h, the top-level call, and one negative entry list `[[not_a_use]]` for the comment and string-literal occurrences with their byte spans.
   Byte offsets are zero-based half-open; lines and columns are one-based; columns are UTF-8 byte columns.
3. Compute every offset mechanically, never by hand. Write `tests/gold/check_gold.py` (stdlib only, under 80 lines) that loads the TOML with `tomllib`, slices each declaration/use/not_a_use span out of the named fixture file as bytes, and asserts the slice equals the recorded `text` (declarations: the first 40 bytes) and that the computed line/column from the offset matches. Exit non-zero on any mismatch and print the failing entry. Run it and paste its real output.
4. Add `tests/fixtures/php/authored/README.md` (under 30 lines) listing the cases a–i, which ones are unresolved by design in v0.1 (g), and noting that `check_gold.py` is the verification.
5. Update docs/TASKS.md: tick T04, Last completed = T04, Next = T05, and note in "Last checks" that `python3 tests/gold/check_gold.py` passes. Do not touch other docs. Do not run git.

Do not build a fixture framework, do not add Rust code, do not add a real repository. Finish with the AGENTS.md final block.
