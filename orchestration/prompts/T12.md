Before T12, apply one review fix from T11 in `crates/rivet-languages/src/php/mod.rs` (do it first, it is small):
Members of an anonymous class (`$o = new class { public function z() {} };`) are currently emitted as parentless top-level symbols with a bare qualified name (`Method z parent=None`). Spec §10.1 says anonymous definitions are not addressable symbols in v0.1. Fix: when a method/property/const's enclosing class-like node is an anonymous class (in the pinned tree-sitter-php grammar check the node kind, likely `anonymous_class` or an `object_creation_expression` containing a `declaration_list` rather than a `class_declaration`), skip the member entirely. Add a regression test in `crates/rivet-languages/tests/php_extract.rs` that parses exactly that snippet plus a closure `$f = function () { return 1; };` and asserts zero symbols. Also assert that a NAMED class declared after the anonymous class in the same file is still extracted with its methods.

Then:

Implement T12 from docs/TASKS.md only. Read AGENTS.md first, then the T12 row, spec §10.2 and §10.3, docs/OUTPUT-CONTRACT.md "Coordinates and symbol objects", "Pagination and resolution", the `rivet symbol` block, and "Errors" (ambiguous_symbol and symbol_not_found rows), and docs/ARCHITECTURE.md "Query execution". Nothing else.

T12 — Persist symbol records and implement `rivet symbol <query> --json` with canonical-ID, short-name, and qualified-name lookup. Call lists remain explicitly unavailable.

Requirements:
1. `rivet-store`: add `replace_file_symbols(path, &[SymbolRow])` (delete then insert within the caller's transaction), `find_symbols_by_lookup_name(name)`, `find_symbols_by_qualified_name(qname)`, `get_symbol(id)`, all returning rows ordered by `(file bytes, start_byte, id)`. `SymbolRow` mirrors the `symbols` table: id, file, name, lookup_name, qualified_name, kind, parent_id, start_byte, end_byte, start_line, end_line, signature, doc_comment. `lookup_name` for PHP: classes/functions/namespaces/methods are case-insensitive in PHP, properties and constants are case-sensitive; implement `rivet_languages::php::lookup_name(name, kind) -> String` (lowercase for the insensitive kinds) and document it in a comment.
2. Indexing path (extend T10's `index`): for each file with `parse_status = ok` and language php, parse with the T02 grammar, run T11 `extract`, assign duplicate ordinals with T03 `assign_ordinals` over `(qualified_name, span, kind)`, build `SymbolId`s, compute lines via `LineIndex`, set `parent_id` from `parent_index`, and persist inside the same publish transaction as the inventory. If extraction returns a `parse_error` or `resource_limit` diagnostic, set the file's `parse_status` accordingly, store no symbols, and include the diagnostic. `symbols` count in `index --json` becomes real. Everything still happens in one transaction.
3. Query resolution in `rivet-index` (add deps on `rivet-core` and `rivet-store`): `resolve_query(store, query) -> Result<Vec<SymbolRow>>` accepting (a) canonical ID (contains an unescaped `#` and parses via `SymbolId::parse`; match by exact id), (b) native qualified name (contains `\` or `::`; match `qualified_name` exactly, then case-insensitively for insensitive kinds), (c) dotted path `A.B.c`: normalize separators `::`, `->`, `\`, `/`, `.` to `.` on both sides and match symbols whose normalized qualified name ENDS WITH the normalized query at a component boundary, (d) short name: match `lookup_name`. Try in that order and stop at the first form that yields matches. `file:line` is T13, not now.
4. CLI `rivet symbol <query> [--json] [--limit N] [--offset N] [--signature-only] [--source]`: discover, config, refresh (reuse T10 indexing so results are current), resolve. Zero matches → exit 4 `symbol_not_found` with `suggestions`: up to 5 distinct qualified names ordered by Levenshtein distance on Unicode scalars then name bytes (implement a small Levenshtein in rivet-index). More than one → exit 5 `ambiguous_symbol` with `total`, `truncated`, `next_offset`, `candidates` (full symbol objects, paginated by `--limit`/`--offset`). Exactly one → success object per the contract: `schema_version`, `index`, `symbol` (full object incl. `start_byte`, `end_byte`, `content_hash` of its file), `signature: null`, `doc_comment: null`, `parent`, and `calls`/`called_by` as `{"total":0,"truncated":false,"next_offset":null,"items":[]}`. IMPORTANT per TASKS: call lists are not yet real, so ALSO add a top-level key `"development_note": "calls and called_by are not implemented until T24"` to the success object for now, so a consumer cannot mistake empty lists for a real answer; T24 removes it. `--source` and `--signature-only` are parsed but `--source` returns the stored bytes slice as `source` (that is trivially available) and `--signature-only` omits the call lists. Reject `--source` with `--signature-only` (exit 2).
5. Integration tests in `crates/rivet-cli/tests/symbol_json.rs` against a temp copy of the authored fixture with a `.git` dir: `rivet symbol App\\Services\\SurveyService::launch --json` succeeds with the gold span; `rivet symbol launch --json` exits 5 with `total == 3` (two methods plus the `App\Boot\launch` function) and candidates sorted by file bytes then start_byte; `--limit 2` gives `truncated == true` and `next_offset == 2`; `rivet symbol SurveyService.launch --json` exits 5 with total 2 (dotted matches both classes named SurveyService? No: only `App\Services\SurveyService::launch` ends with `SurveyService.launch`, so expect total 1 and success; assert that); `rivet symbol Nope --json` exits 4 with a `suggestions` array; canonical id round trip: take `symbol.id` from a success and query it again, same result; stdout is empty on errors and stderr is empty on success.
6. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`. Paste real results.
7. Update docs/TASKS.md: tick T12, Last completed = T12, Next = T13, one-line "Last checks". Do not touch other docs. Do not run git.

Finish with the AGENTS.md final block.
