IMPORTANT: you are working in a git worktree on branch `task/T14`, in parallel with T13 (query forms in rivet-index) and T17 (PHP use extraction, which adds `crates/rivet-languages/src/php/uses.rs` and hooks in `php/mod.rs`). To keep merges clean: put signature/doc-comment logic in a NEW file `crates/rivet-languages/src/php/signature.rs` and only add a `mod signature;` line plus a single call site in `php/mod.rs`. Put CLI changes for `--source`/signature output in the symbol command module only. Add the fourth fixture file rather than editing existing fixture bytes. Tests go in a NEW file `crates/rivet-cli/tests/symbol_source.rs`. In `docs/TASKS.md` tick T14 only and add a note on the "Active task / partial progress" line; do not change "Last completed"/"Next". Do not run git.

Implement T14 from docs/TASKS.md only. Read AGENTS.md first, then the T14 row, spec §16.2 (collapsed form definition) and §27 paragraph on stored bytes, docs/OUTPUT-CONTRACT.md `rivet symbol` block, and docs/ADDING-A-LANGUAGE.md "Adapter contract" (the `signature` method). Nothing else.

T14 — Declaration signatures and doc comments for PHP, and `symbol --source` from stored bytes.

Requirements:
1. In the PHP adapter (`rivet-languages::php`), fill `ExtractedSymbol.signature` and `doc_comment`:
   - `signature`: the declaration header text exactly as written in the source, from the node start (including modifiers) through the end of the return type or the last token before the body `{`, with internal newlines and runs of whitespace collapsed to single spaces and no trailing whitespace. For classes/interfaces/enums: modifiers, keyword, name, and any `extends`/`implements` clause. For methods/functions: through the return type (`public function launch(Survey $s): void`). For properties: the full one-line declaration without the trailing `;` (`private string $label = 'survey'`). For constants: `public const DEFAULT_LABEL = 'survey'`. For namespaces: `namespace App\Services`.
   - `doc_comment`: the text of an immediately preceding `/** ... */` comment node (only docblocks, not `//` or `/* */`), with the delimiters kept and leading indentation of each line trimmed; `None` when absent. A blank line or another statement between comment and declaration means no docblock.
2. Implement the container **collapsed form** now, as `rivet_languages::php::signature_summary(symbol, members, source) -> String`: for a class/interface/enum, emit the class signature line, `{`, then one line per direct member (`    <member signature>;` for properties/constants and `    <method signature> { … }` for methods, using the ellipsis character `…`), then `}`. For a non-container symbol return its `signature` unchanged. Deterministic member order = source order. Add unit tests on the fixture's `SurveyService` class: exact expected string.
3. Persist `signature` and `doc_comment` into the `symbols` table (columns exist) during indexing and return them in `rivet symbol --json` (`signature` non-null now, `doc_comment` null unless present). Add a docblock to one method in `tests/fixtures/php/authored/SurveyService.php` ONLY IF you also update `tests/gold/php-authored.toml` byte offsets by re-running the mechanical computation and `python3 tests/gold/check_gold.py` passes; if that is more than a small change, instead add a NEW fourth fixture file `Documented.php` with a docblocked class and method and gold entries for it. Prefer the fourth file.
4. `rivet symbol <q> --source`: return `source` as the exact stored bytes for the symbol's span (from `files.source`, never from the live file), as a UTF-8 string. Add an integration test that indexes, then modifies the live fixture file on disk WITHOUT re-running index (use `--no-refresh`? Not implemented until T16, so instead: copy the fixture, run `rivet symbol X --source --json`, then overwrite the file and run again; the second run WILL refresh and see the new content in T14. Therefore the test must assert the opposite for now: after an edit, a fresh query returns the NEW span and the response's `symbol.content_hash` changed. Note in a comment that snapshot-vs-live divergence is tested in T15/T16 once refresh modes exist.) Also assert `--source` output for a CRLF fixture file (create one in the temp copy) preserves `\r\n` bytes and that a file with a multibyte character before a method still returns the exact method text.
5. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, `python3 tests/gold/check_gold.py`. Paste real results.
6. Update docs/TASKS.md: tick T14, Last completed = T14, Next = T15, one-line "Last checks". Do not touch other docs. Do not run git.

Finish with the AGENTS.md final block.
