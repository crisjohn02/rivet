Implement AF5 from docs/TASKS.md only. This is an audit-fix task, not new scope. Read AGENTS.md first, then `orchestration/review-notes/audit-2026-09-22.md` findings 14, 15, 16 and 17 and its second unverified suspicion, the AF5 row in docs/TASKS.md, `docs/OUTPUT-CONTRACT.md` sections "Flag applicability", "Common index metadata", "Administrative commands" (the `index --json` counts), and "Errors", `docs/ARCHITECTURE.md` "Refresh and invalidation", "Concurrency and source consistency" and "Parse and coverage policy", `crates/rivet-parser/src/lib.rs`, `crates/rivet-cli/src/refresh.rs`, `crates/rivet-cli/src/symbol.rs`, `crates/rivet-cli/src/refs.rs`, `crates/rivet-cli/src/transport.rs`, `crates/rivet-store/src/lib.rs` `publish_inventory`, and `crates/rivet-core/src/walk.rs`. Nothing else.

`docs/OUTPUT-CONTRACT.md` is normative. Where this prompt and the contract disagree, the contract wins and you report the disagreement.

AF5 — Contract and coverage honesty. Each item makes rivet's metadata claim something that is not true.

The defects:

1. Finding 14. A TypeScript file counts as indexed although nothing extracts it, even when it does not parse. TypeScript is a default feature, but TypeScript extraction does not exist yet; it is deferred to T41 onwards. An invalid `a.ts` reports `files_indexed: 1`, `complete: true`, zero symbols and no `parse_error`. That claims coverage rivet does not have. Until TypeScript extraction exists, a TypeScript file must not count as indexed. Choose the skip reason the contract supports, report why, and make `complete` false whenever such a file is present. This must not change a PHP-only repository at all.
2. Finding 15. An index-dependent error omits `index` even when a snapshot was acquired. The contract's Errors section says: "Index-dependent errors also include `index` if a compatible snapshot was successfully acquired." `rivet symbol nosuch --json` exits 4 with no `index` key although a refresh succeeded first, and the ambiguity error has the same fault. Determine which errors are index-dependent and include `index` exactly when a snapshot was acquired. An argument error raised before any filesystem work must still carry no `index`.
3. Finding 16. `updated` does not count files whose facts were regenerated. The contract says `updated` counts current file rows with "changed source/status/language or regenerated facts". After an extractor-fingerprint change forces a reparse, every file's facts are regenerated, yet `index --json` reports `updated: 0`. Count them.
4. Finding 17. The non-UTF-8 path diagnostic uses the absolute, lossily decoded path as its `file`, instead of a repository-relative path. APFS on this machine refuses non-UTF-8 filenames, so this cannot be reproduced here by creating a file. Fix it by reading the code, test the path conversion as a unit where possible, and state plainly that the end-to-end case is untestable on this filesystem. The existing test at `walk.rs` only checks that the diagnostic contains `bad-`, which passes whatever the path form; make it assert the relative form on a filesystem that supports the name.
5. Confirmed by the orchestrator, previously an unverified audit suspicion. `--no-refresh` answers from a cache built by a different extractor. With the stored `extractor_fingerprint` altered, `rivet symbol one --no-refresh --json` exits 0 with `freshness: cached`. The flag table says `--no-refresh` "requires compatible cache", and the Errors table assigns incompatible-cache failures to `repository_unavailable`, exit 3. A cache whose extractor or resolver fingerprint differs from this build's must be refused with that error, since its facts were produced by different rules; a cache built before the recent audit fixes would otherwise serve the wrong exact bindings those fixes removed. State exactly which stored fingerprints you compare and why.

Requirements:

1. Every change must match the contract text it cites. Quote the clause in a doc comment where the behavior is implemented.
2. Do not change resolution, bindings, symbols or uses. `python3 tests/gold/check_gold.py` must verify 51 entries and the authored fixture's 13 bindings must be unchanged. The only expected differences are in coverage metadata, counts and error envelopes.
3. The authored fixture contains a non-PHP file, `README.md`, which already makes `complete` false through the `unsupported` count. Do not disturb that.
4. Tests, one per item, asserting exact JSON values rather than presence alone: an invalid `.ts` file's coverage and `complete: false`; a valid `.ts` file likewise; `index` present on `symbol_not_found` and `ambiguous_symbol` after a refresh and absent on an argument error; `updated` equal to the reparsed file count after a fingerprint change and zero on a no-change refresh; the relative path form of the non-UTF-8 diagnostic; and `--no-refresh` refusing a cache with an altered extractor fingerprint, and separately an altered resolver fingerprint, with exit 3, while still answering from a compatible cache. Put end-to-end coverage in a new sibling file under `crates/rivet-cli/tests/`, and say which.
5. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts.
6. Update docs/TASKS.md: tick AF5 and update "Last checks" only. Do NOT change "Last completed task" or "Next task". Do not touch other docs. Do not run git.

Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
