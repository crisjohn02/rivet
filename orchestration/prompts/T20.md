Implement T20 from docs/TASKS.md only. Read AGENTS.md first, then the T20 row, spec §11.3 (the `scoped` row) and §11.4 items 1 and 3; docs/ADDING-A-LANGUAGE.md "MVP support boundary" (PHP rows Local receiver hints and Not resolved in v0.1); and the existing resolver in `crates/rivet-index/src/resolve/` (mod.rs and rules/imports.rs) plus the persisted `UseHint` shapes in `crates/rivet-core/src/extract.rs`. Nothing else.

T20 — Resolve `$this`/`self` member uses and explicit native receiver types to `scoped` bindings. Add ONE rule file; do not restructure the resolver.

Requirements:
1. Add `crates/rivet-index/src/resolve/rules/receivers.rs` and register it in `rules/mod.rs` AFTER `imports` and `functions`, per the order already documented in `resolve/mod.rs`. Do not modify `imports.rs` or `functions.rs`.
2. Rule 1, `$this` / `self` / `static` (hint `This` or `SelfOrStatic`): the use's containing symbol identifies the enclosing class; bind the member named by the use's spelling to the member declared in THAT class, matching on the PHP case rule (methods case-insensitive, properties and constants case-sensitive, properties keep the `$`). Resolution is `scoped`, never `exact`: late static binding and inheritance can select another implementation at runtime (spec §11.3). If the enclosing class does not declare that member, record NOTHING (v0.1 does not traverse inheritance; ADDING-A-LANGUAGE lists inheritance under "Not resolved"). `static::` behaves like `self::` for the declaring class but must also record nothing when the member is absent.
3. Rule 2, explicit receiver type (hint `Typed { type_spelling }`): resolve `type_spelling` to a class symbol using the SAME scope-chain logic `imports.rs` already uses (alias import, fully qualified, then namespace-relative). Reuse that logic by extracting it into a shared helper in `resolve/mod.rs` or `rules/mod.rs` and calling it from both files; do not copy it. If it resolves to exactly one class AND that class declares the member named by the use's spelling, bind `scoped`. Otherwise nothing.
4. Never upgrade to `exact`. Conflicting or multiple candidates record nothing. A use that already has a binding from an earlier rule is skipped by the existing first-wins dispatcher; do not special-case it.
5. Tests. Unit tests in `receivers.rs` against an in-memory store for: `$this->launch()` inside the declaring class; `self::CONST` read; `$this->missing()` where the class has no such member (no binding); a typed parameter whose type is an aliased import; a typed parameter whose type is not indexed (no binding). Extend `crates/rivet-cli/tests/bindings.rs` on the authored fixture asserting the new totals: `SurveyService.php:26` `$this->launch()` binds `scoped` to `App\Services\SurveyService::launch`; `SurveyService.php:20` `$this->label` write binds `scoped` to the property and the `self::DEFAULT_LABEL` read binds `scoped` to the constant; `ReportService.php:30` `$svc->launch()` (typed parameter) binds `scoped` to `App\Services\SurveyService::launch`; `ReportService.php:36` `$x->launch()` stays UNBOUND; the `new`-hint calls at `ReportService.php:24`, `boot.php:17` and `boot.php:22` stay unbound (those are T21). State the exact expected `bindings` count and assert it.
6. Add an adversarial test in a temp fixture: two classes in different namespaces both declaring `launch`, and a method with a typed parameter of one of them calling `$p->launch()` — it must bind to the parameter's class only, never the other. Also: a class whose parent declares the method while the child does not (no binding in v0.1, and a comment naming inheritance as future work).
7. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`. Paste real results.
8. Update docs/TASKS.md: tick T20, Last completed = T20, Next = T21, one-line "Last checks". Do not touch other docs. Do not run git.

Finish with the AGENTS.md final block.
