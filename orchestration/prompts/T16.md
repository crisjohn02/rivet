Implement T16 from docs/TASKS.md only. Read AGENTS.md first, then the T16 row, spec §12.4 (all, including the `--no-refresh` paragraph), §22 (freshness key), and §13 (`--force`); docs/OUTPUT-CONTRACT.md "Flag applicability" (freshness, no-refresh, force rows) and "Common index metadata" (`freshness` values); docs/ARCHITECTURE.md "Refresh and invalidation" (fingerprint and invalidation paragraphs) and the `--no-refresh` paragraph in "Concurrency and source consistency"; and `crates/rivet-cli/src/refresh.rs`. Nothing else.

T16 — Effective-config and grammar invalidation, explicit `metadata` and `cached` freshness modes, and real `--force`.

Requirements:
1. Invalidation by fingerprint. `meta.effective_config_fingerprint` (from `Config::fingerprint()`), `meta.extractor_fingerprint`, and `meta.resolver_fingerprint` are compared at the start of every refresh:
   - config fingerprint changed (ignores, excludes, enabled languages, max size, freshness default): re-walk and re-evaluate eligibility for every path; files that became excluded or unsupported lose their facts; files that became eligible are read and parsed.
   - extractor fingerprint changed: reparse every enabled-language file (T15 already does this; keep it here as the single place).
   - resolver fingerprint changed: no-op until T22 (bindings do not exist yet); leave a clearly named hook.
   The three current fingerprints are always written back by the publish.
2. `--freshness content|metadata` on `index`, `symbol` (and later `refs`, `context`): CLI flag overrides `config.index.freshness`. `metadata` mode: for a file whose stored `(mtime_ns, size)` equal the walked entry's, reuse the stored `content_hash` and skip reading; otherwise read and hash as in content mode. The response's `index.freshness` reports the mode actually used. Document in a code comment that metadata mode can miss a same-size edit with a restored mtime, and add a test proving exactly that (the T15 same-size test inverted: under `--freshness metadata` the OLD byte is returned; under default content mode the NEW byte is returned).
3. `--no-refresh` on `symbol` (and later query commands, never `index`): open the store and answer from the committed snapshot with NO walk, read, or parse. `index.freshness` is `cached`. Requires a compatible existing `index.db`; if `.rivet/index.db` does not exist or `IncompatibleIndexFormat` → exit 3 `repository_unavailable` with a hint to run `rivet index`. `--no-refresh` together with `--freshness` → exit 2. `index --no-refresh` → exit 2.
4. `--force` on `index`: delete all `files`, `symbols`, `uses`, `bindings`, `scopes`, `diagnostics` rows and rebuild everything inside the same single publish transaction (never delete the database file, never touch config). The report's `updated` equals `files_seen`. If the store is `IncompatibleIndexFormat`, `--force` may drop and recreate the schema (it is a disposable cache) after `validate_destination` passes; without `--force` that remains exit 3 with a hint mentioning `--force`.
5. Tests in `crates/rivet-cli/tests/freshness_modes.rs` on a temp fixture copy:
   - config invalidation: index; write `.rivet/config.toml` with `exclude = ["boot.php"]`; `rivet symbol App\Boot\launch --json` exits 4 and `index --json` `files_seen` dropped by one; remove the exclusion and it returns.
   - language invalidation: set `enabled = ["typescript"]`; PHP symbols disappear and `skipped.unsupported` grows accordingly; restore and they return.
   - metadata mode misses a restored-mtime edit; content mode catches it (see item 2).
   - cached: index, then delete `boot.php` on disk, then `rivet symbol App\Boot\launch --no-refresh --json` STILL succeeds with `index.freshness == "cached"`, while the same query without `--no-refresh` exits 4.
   - cached without an index: fresh temp repo with `.git` but no `.rivet` → `rivet symbol X --no-refresh --json` exits 3 with the hint.
   - `--no-refresh --freshness metadata` → exit 2; `index --no-refresh` → exit 2.
   - `--force`: after a normal index, `index --force --json` reports `updated == files_seen`, `unchanged == 0`, and the same `snapshot` digest as before (content unchanged).
6. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`. Paste real results.
7. Update docs/TASKS.md: tick T16, Last completed = T16, Next = T17, one-line "Last checks". Do not touch other docs. Do not run git.

Finish with the AGENTS.md final block.
