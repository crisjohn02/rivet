Review fix first (before T30). T28 added the `budget_too_small` error, exit 8, but left `CliError::is_index_dependent` in `transport.rs` treating it as not index-dependent, so it will not carry `index`. The contract's Errors section says "Index-dependent errors also include `index` if a compatible snapshot was successfully acquired". Error 8 can only be computed from the snapshot, since `required_tokens` is the size of a stored symbol's source, so it is index-dependent. Make it carry `index` whenever a snapshot was acquired, as AF5 did for `symbol_not_found` and `ambiguous_symbol`, and add a test.

Implement T30 from docs/TASKS.md only. Read AGENTS.md first, then the T30 row; `docs/OUTPUT-CONTRACT.md` sections "Transport and common rules", "Flag applicability", "Coordinates and symbol objects", "Common index metadata", "Pagination and resolution", "`rivet context <query> --tokens N --json`", "Errors", and "Ordering and versioning"; spec §16 and §16.5; `crates/rivet-cli/src/main.rs`, `symbol.rs`, `refs.rs`, `transport.rs`, `refresh.rs`, `context.rs` (T26/T27) and `budget.rs` (T28/T29); and `crates/rivet-core/src/config.rs` `ContextConfig`. Nothing else.

`docs/OUTPUT-CONTRACT.md` is normative. Where this prompt and the contract disagree, the contract wins and you report the disagreement.

T30 — Wire `rivet context <query>` end to end: arguments, discovery, refresh, query resolution, candidate collection, fitting, and JSON output. Everything below the CLI already exists; this task connects it. Do not rewrite collection or fitting.

Requirements:

1. Add `crates/rivet-cli/src/context_cmd.rs`, or a name you justify, following the `symbol.rs` and `refs.rs` pipeline exactly: validate every argument before any filesystem work, open the context, validate configured languages, refresh or use the cached store for `--no-refresh` (keeping AF5's compatibility check), resolve the query handling every `QueryOutcome` arm as `symbol` does, then call `collect_ranked` and `fit_collection`, then emit JSON. Reuse the existing helpers rather than copying them.

2. Flags, per "Flag applicability" and spec §16.5:
   - `--tokens N`: a positive integer up to 1,000,000; default from `ContextConfig.default_token_budget`, which defaults to 4,000.
   - `--depth 1|2`: default from `ContextConfig.max_depth`, otherwise 2.
   - `--collapse auto|always|never`: default from config, otherwise `auto`.
   - `--include-tests` and `--exclude-tests`: mutually exclusive; default from config, otherwise include.
   - `--include-callers` and `--exclude-callers`, and `--include-callees` and `--exclude-callees`: each pair mutually exclusive; default include.
   - `--limit N`: the segment count including the target; default 50, range 1 to 1,000 as in "Pagination and resolution".
   - `--offset`: REJECTED for context, with `invalid_arguments`, exit 2.
   - `--freshness content|metadata` and `--no-refresh`, which conflict with each other, as in `symbol` and `refs`.
   - `--json`.
   Every invalid value and every conflicting pair is `invalid_arguments`, exit 2, raised before any filesystem work.

3. JSON, with keys in exactly the contract's order: `schema_version`, `index`, `symbol`, `budget_tokens`, `estimated_tokens`, `tokenizer`, `budget_scope`, `segments`, `omitted`, `candidate_limit_reached`. `tokenizer` is the literal `utf8-bytes-v1` and `budget_scope` the literal `source`. Each segment has `symbol` (the full symbol object, reusing the existing builder), `form`, `reason`, `resolution`, `estimated_tokens`, `source`, in that order. `omitted` has `budget`, `overlap`, `limit` in that order; note that this is the KEY order from the contract's shape, while overlap, limit, budget is the assignment PRECEDENCE, already implemented in T29. The target segment's resolution is `exact`.

4. Errors. Ambiguity returns `ambiguous_symbol`, exit 5, with its candidates at offset zero, since context rejects `--offset`; per the contract, the hint may direct the caller to `rivet symbol` for further pages. Not found is `symbol_not_found`, exit 4, with suggestions. A target that cannot fit is `budget_too_small`, exit 8, with `budget_tokens` and `required_tokens`, carrying `index` per the review fix above.

5. Remove the not-implemented placeholder for `context`: its arm in `main.rs` and its mention in the not-implemented hint, which should list the commands now implemented. `crates/rivet-cli/tests/index_json.rs::unimplemented_command_emits_json_error` asserts `context` is unimplemented and WILL break. Retarget it to `init` or `snippet`, which remain unimplemented until T33, rather than deleting the coverage, and say which.

6. Human output is T34. Without `--json`, follow whatever `refs` and `symbol` currently do without it; do not build new human formatting.

7. Out of scope, do not fix, but do not make worse: an invalid `file:line` query is rejected after refresh, and `file:line` on an unsupported file exits 4 instead of 7. Both belong to T32.

8. Tests, in a new `crates/rivet-cli/tests/context_json.rs`, driving the real binary. Assert exact values, not presence alone. Cover:
   a. The done-when: a single `rivet context` invocation on the authored fixture, with no earlier `index`, `symbol` or `refs` call, returns the target plus its related source. Use `SurveyService::launch`, which has callers and a parent, and assert the exact ordered reasons and symbol IDs.
   b. Top-level and segment key order against the contract, with `tokenizer` and `budget_scope` literals.
   c. `estimated_tokens` equals the sum of the segments' estimates and never exceeds `budget_tokens`, across a sweep of `--tokens` values run through the binary.
   d. Each flag changing the result as the contract says: `--depth 1` removes `second_degree`; each exclusion removes its relationship; `--collapse never` yields only `full` forms; `--limit 1` yields only the target and counts the rest under `limit`.
   e. Each mutually exclusive pair, `--offset`, `--tokens 0`, `--tokens 1000001`, `--limit 0`, `--limit 1001`, `--depth 3` and an unknown `--collapse` value: each `invalid_arguments`, exit 2, with empty stdout and no filesystem work, proved by running in a directory that is not a repository.
   f. `ambiguous_symbol` at offset zero, `symbol_not_found` with suggestions, and `budget_too_small` with `required_tokens` and `index`.
   g. `--no-refresh` labeled `cached`, and refused with exit 3 on an incompatible cache.
   h. Determinism: the same invocation twice is byte-identical.
9. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts.
10. Update docs/TASKS.md: tick T30, Last completed = T30, Next = T31, one-line "Last checks". Do not touch other docs. Do not run git.

If this grows beyond one coherent change, split as T30a/T30b, record what works and what remains, and do not tick T30. Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
