Implement T36 from docs/TASKS.md only. Read AGENTS.md first, then the T36 row; `docs/IMPLEMENTATION-PLAN.md` "Correctness acceptance matrix" (this task's checklist); `docs/OUTPUT-CONTRACT.md` in full; spec §20 (Output Contract), §21 (Exit Codes), §25, §26 and §27; `docs/BUILDING.md` its test layout table; `orchestration/review-notes/spec-issues.md` and `orchestration/review-notes/audit-2026-09-22.md` "Follow-ups surfaced while fixing"; and the existing tests under `crates/rivet-cli/tests/`. Read source files only as needed to trace a case.

`docs/OUTPUT-CONTRACT.md` is normative. Where this prompt and the documents disagree, they win and you report the disagreement.

T36 — Audit the CLI contract and the correctness acceptance matrix, and close omissions with focused regression cases. This is an audit that produces tests, not a feature. The T36 row: "Flags/errors/stream behavior, worktrees, symlink/FIFO handling, rebuild equivalence, Unicode, and partial coverage agree with the docs. Record any remaining gap as a subtask before proceeding."

Requirements:

1. Build the map first. For every case in every row of the correctness acceptance matrix, which has ten areas from Freshness to Determinism, find the existing test or tests that prove it, by file and test name, or record that none does. Write the map to a new `crates/rivet-cli/tests/ACCEPTANCE.md`, one line per case: the case, the test or tests, and a status of covered, added in T36, gap recorded as a subtask, or not applicable with the reason. This file is the task's main deliverable. Do not mark a case covered unless you read the test and it genuinely asserts the required result, not merely something nearby.
2. Close the gaps with focused regression tests that drive the real binary and assert exact outcomes. Expect gaps at least in these, which earlier reviews flagged as thinly covered:
   - Filesystem: a Git worktree whose `.git` is a file; a nested repository or submodule, which is excluded; a symlinked `.rivet`; a symlink pointing outside the root, which must never be followed; a FIFO in the tree, which must never be opened, so the walk must not hang, with a timeout guard in the test itself; CRLF and non-ASCII Unicode source with correct byte, line and column coordinates; and a permission-denied file failing refresh with exit 3.
   - Identity: literal `#` and `%` in a path, escaped in IDs per spec §10 with no collision; duplicate declarations getting distinct ordinals; nested definitions on one line; and paths differing only in case. Note that this machine's filesystem is case-insensitive, and say what you could and could not test.
   - Output and streams: for every command, success prints exactly one JSON object and a newline on stdout with nothing on stderr, and failure prints exactly one JSON error object on stderr with nothing on stdout, per spec §20; empty results; limit and offset at exact boundaries; ambiguity pages.
   - Determinism: a clean rebuild versus an incremental refresh giving identical query bytes; repeated queries.
3. Fix what you can, record what you cannot. When a test exposes behaviour that disagrees with the documents: if the fix is small and clearly within the contract, fix it and say so; if it is larger, or needs a document decision, leave the test marked `#[ignore]` with a reason naming a new subtask ID, T36a, T36b and so on, and add that subtask as a row in docs/TASKS.md with its done-when. Never weaken a test to make it pass, and never silently change documented behaviour.
4. Carry these known items into the map, deciding for each whether it is a gap, a documented limitation, or a spec issue:
   - A valid PHP file whose heredoc body contains `<?php` is reported as a parse error. It was found in a private corpus; reproduce it with a small authored file of your own and nothing from that corpus. Determine whether the cause is the pinned Tree-sitter grammar or rivet's parse policy, and say which.
   - rivet excludes nested `vendor/` directories, not only the root one, which drops customised Laravel package templates. Spec §26 does not say which is intended; it is a spec issue.
   - `docs/BUILDING.md` names test files that do not exist, such as `freshness.rs`, `coverage.rs`, `determinism.rs` and an `insta`-based `snapshots/` directory. Map each named suite to where its coverage actually lives, and record the mismatch. You may not edit BUILDING.md.
5. Do not change any successful output. The authored fixture's `index`, `symbol`, `refs` and `context` output must stay byte-identical, digest included, unless a fix under item 3 changes it, in which case say exactly what changed and why. Gold must verify 51 entries.
6. Use only authored fixtures and temporary directories you create. Do not use the private benchmark corpus under /Users/cris/rivet-corpus/ or anything under /Users/cris/ssr/.
7. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate counts, and list every `#[ignore]`d test with its subtask.
8. Update docs/TASKS.md: tick T36 only if every gap is either closed or recorded as a subtask row, set Last completed = T36 and Next = T37, and add a one-line "Last checks". Do not touch other docs beyond the new subtask rows. Do not run git.

If this grows beyond one coherent change, split it, record what works and what remains, and do not tick T36. Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
