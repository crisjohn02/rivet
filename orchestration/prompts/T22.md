Review fix first (before T22). T21 left a real invalidation defect that T22 is the right task to close. `EXTRACTOR_FINGERPRINT` in `crates/rivet-languages/src/lib.rs` encodes only grammar versions ("php=0.24.2;ts=0.23.2"), so it did not change when T21 changed what the extractor *records*: `new_bindings` went from recording only direct `new` assignments to recording every simple assignment, `NewBinding` gained `direct_new` and `block`, and `UseHint::NewExpr` gained `use_block`. Because the fingerprint is unchanged, a repository indexed by the previous binary is reused without reparsing, and the serde defaults (`direct_new = true`, `block = None`, `use_block = None`) make every stale fact look like an unconditional direct `new`.

Reproduced by hand, not theorised. With `$t` assigned inside an `if` body and used after it, indexing with the pre-T21 binary and then re-indexing with the T21 binary yields a `scoped` binding for `$t->go()`, while a fresh T21 index of byte-identical content leaves it unbound: 6 bindings versus 5. That breaks the determinism non-negotiable, since the same content gives different output depending on index history, and the honesty non-negotiable, since the binding has no justifying rule.

Fix it as part of this task:
- Make the extractor fingerprint cover the recorded fact *schema*, not just grammar versions. Add a fact-schema component that this change bumps, so any future change to recorded fact shape or meaning invalidates stored facts. Say in your report exactly what the new fingerprint value is and what must change it in future.
- Add a regression test that fails against the current code: build facts in the old shape (or persist a snapshot with an older fingerprint), re-index, and assert the conditional-assignment case stays unbound and that the binding set equals a from-scratch index of the same content.
- State plainly whether the serde defaults should remain. If stale facts can no longer reach the resolver, say so and justify keeping or removing the defaults; do not leave a default that would silently mean "safe" if it ever were reachable.

Implement T22 from docs/TASKS.md only. Read AGENTS.md first, then the T22 row, spec §12.3 (Incremental Indexing) and §12.4 (Automatic Freshness); docs/ARCHITECTURE.md "Refresh and invalidation" (especially the pseudocode line `if content, membership, or resolver fingerprint changed: clear bindings; re-resolve all persisted uses/scopes`) and "Resolution and context"; `crates/rivet-cli/src/refresh.rs`; `crates/rivet-store/src/lib.rs` `publish_inventory` and the `bindings` table; and the existing tests `crates/rivet-cli/tests/bindings.rs`, `tests/refresh.rs`, `tests/freshness_modes.rs`. Nothing else.

T22 — Re-resolve all persisted uses when indexed content or membership changes. Most of the machinery already exists: `publish_inventory` unconditionally deletes the whole `bindings` table and reinserts, and reused unchanged files contribute their stored uses/scopes to the resolver input. Your job is to close the gaps and prove the behavior, NOT to rebuild the refresh path.

Requirements:

1. Close the resolver-fingerprint gap. `refresh.rs` computes `let _resolver_changed = ...` around line 233 and deliberately discards it, with a comment naming T22 as the task that uses it. Determine, by reading the code and then by test, whether any path can answer a query from a snapshot whose bindings predate a resolver-rule change. Specifically check whether a refresh that finds no content and no membership change can skip `publish_inventory` and therefore keep stale bindings. If such a path exists, make a changed `RESOLVER_FINGERPRINT` force a re-resolve and publish; if no such path exists, replace the discarded binding with a comment stating precisely why the fingerprint needs no separate trigger, and prove it with a test. Do not restructure `refresh_inventory`. Report which of the two you found and the evidence.

2. Do NOT weaken the reuse path. Unchanged files must still not be reparsed. The debug-only `record_reparsed` hook test in `tests/refresh.rs` must still pass, and you must add an assertion that re-resolution happens even while no file is reparsed.

3. Tests are the substance of this task. Add them to `crates/rivet-cli/tests/` (extend `bindings.rs` or add `reresolve.rs`; state which and why). Every scenario below must change a file OTHER than the one holding the use, then re-index, then assert the binding in the unchanged file changed. Existing coverage only ever edits the file containing the use, so none of these are covered today.
   a. Duplicate declaration invalidates an exact binding. A file imports and calls a unique target and binds `exact`. Add a NEW file declaring a same-name competing declaration so the candidate set is no longer unique. Re-index. Assert the use row in the untouched file survives with the same `use_id`, and that its binding is now absent. Then delete the new file, re-index, and assert the `exact` binding returns.
   b. Changed import in another file. Arrange a use whose resolution depends on an import declared in a different file than the use, or, if the PHP rules make that impossible, state that plainly in your report and instead assert the nearest real case: an import change in file A alters bindings for uses in A while leaving unrelated bindings in untouched file B byte-identical.
   c. Deleted target. A use binds to a declaration in another file. Delete that file. Re-index. Assert the binding is gone, the use row still exists with its `use_id` unchanged, and the query path reports the use as unresolved rather than erroring. Restore the file and assert the binding comes back.
   d. Unresolved uses survive. Across every scenario above, assert that the count of `uses` rows for untouched files never drops and that no `use_id` for an untouched file is ever reassigned.

4. Determinism. After each re-index, assert the full `Vec<BindingRow>` from `store.list_bindings()` is byte-identical to a second consecutive re-index with no edit, as `bindings.rs` already does for the authored fixture.

5. Never upgrade a tier. Assert no scenario produces `exact` for a receiver-based call. If a candidate set becomes ambiguous, the result is no binding, never a guess.

6. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results, including the exact per-crate test counts.

7. Update docs/TASKS.md: tick T22, Last completed = T22, Next = T23, one-line "Last checks". Do not touch other docs. Do not run git.

If any requirement contradicts what the code or documents actually say, report the contradiction in your final message rather than silently resolving it.

Finish with the AGENTS.md final block.
