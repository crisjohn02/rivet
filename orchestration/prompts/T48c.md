# T48c: author held-out tasks, batch 2 (h11–h20)

Read `AGENTS.md` first. Then read only:

- `orchestration/prompts/T48b.md` in full. Every rule in it applies here unchanged: privacy and
  isolation, the design, eligibility, the task files, gold and cross-checks, validation, and the
  public-safe report.
- The private batch-1 outputs under `/Users/cris/rivet-corpus/gold/heldout/`:
  - `README.md`;
  - `selection/assignment.md`, `selection/log.md` and `validate.py`;
  - one finished batch-1 task per check type, as the house style.
- Any review notes the orchestrator appended below.

## What this batch does

- **Tasks:** write h11–h20 from the targets **already assigned** in
  `selection/assignment.md`, as the third and fourth target of each category in assignment order:
  - locate: h11, h12
  - callers: h13, h14
  - tests: h15, h16
  - trace: h17, h18
  - dependencies: h19, h20
- **No re-selection.** Do not re-run the selection or pick new targets.
  - If an assigned target turns out to be unusable, record why in `selection/log.md`, using only
    the rejection reasons T48b allows.
  - Replace it with the next candidate in `selection/order.txt` after the last one examined that
    meets that category's eligibility. Record the replacement in `assignment.md`.
- **Validation.**
  - Validate all 20 tasks together at the end, h01–h20, through `run_study.py validate`, using a
    scratch manifest outside every repository.
  - Extend `validate.py` so its self-test covers the new tasks.
- **Hashing.** When all 20 validate, print the task-set hash with
  `python3 /Users/cris/rivet/benchmark/runner/run_study.py hash-tasks <scratch manifest>`.
  Report the hash. It is a SHA-256 over file hashes, so it is public-safe. After that, do not
  modify any task file.

## Review notes from batch 1

(appended by the orchestrator before launch)

1. **Batch 1 passed review.** The orchestrator re-validated h01–h10 and verified two gold
   answers (one callers task and one trace task) by reading the code.
2. **Keep batch 1's conventions exactly**, so the 20 tasks are one design:
   - the trace start-point rule recorded in the README;
   - the fixed-order reading of the rotation;
   - the exact `ClassName::methodName` answer format spelled out in every prompt;
   - the same prompt preamble.
3. **Stay out of git.** Do not run git commands in the corpus copy or in the rivet checkout; use
   plain file reads. Batch 1's read-only git use changed nothing, but it is not needed.
4. **Scratch goes under `/Users/cris/rivet-corpus/gold/heldout/`**, as batch 1 did. The study
   sandbox denies that directory. Report any file you wrote anywhere else so the orchestrator can
   remove it.
5. **Carry batch 1's "did not fit" list forward in the README** and add batch 2's items. The
   preregistration will disclose them.
