Implement T08 from docs/TASKS.md only. Read AGENTS.md first, then the T08 row, docs/ARCHITECTURE.md "Minimal logical schema", "Concurrency and source consistency" (first paragraph), and spec §27 second sentence ("Reject symlinked .rivet, database/config files, and write destinations"). Nothing else.

T08 — SQLite schema and minimal store operations in `rivet-store`. No walking, no parsing, no CLI wiring.

Requirements:
1. Add `rusqlite` with features `["bundled"]` pinned `=x.y.z` to `rivet-store`, plus a path dependency on `rivet-core`. No other dependency.
2. `Store::open(rivet_dir: &Path) -> Result<Store>`: validates the destination first: `rivet_dir` must exist, be a real directory (not a symlink), and `index.db` if present must be a regular non-symlink file; otherwise return an error naming the path. Opens `index.db`, sets `PRAGMA journal_mode = WAL`, `PRAGMA foreign_keys = ON` (verify with `PRAGMA foreign_keys` returning 1 and test it), `PRAGMA busy_timeout = 5000`. Also `Store::open_in_memory()` for tests.
3. On open, read `meta.index_format_version`. If the database is empty, create the full schema exactly as written in ARCHITECTURE "Minimal logical schema" (tables meta, files, symbols, uses, bindings, scopes, diagnostics with the same columns, constraints, and indexes) and set `index_format_version = "1"`. If the value is present and equals "1", proceed. If it is a different value, return `Error::IncompatibleIndexFormat { found }` and do NOT modify the file. No migrations framework.
4. Minimal operations, all inside explicit transactions: `upsert_file(&FileRow)` where `FileRow { path, language: Option<String>, mtime_ns: i64, size: u64, content_hash: Option<String>, source: Option<Vec<u8>>, parse_status }`. Note `walk::FileEntry.mtime_ns` is `i128`; SQLite integers are 64-bit, so provide a documented conversion that clamps to `i64::MIN..=i64::MAX` (nanoseconds since the epoch fit in i64 until the year 2262) and unit-test the clamp; `get_file(path) -> Option<FileRow>`; `list_files() -> Vec<FileRow>` ordered by path bytes; `delete_file(path)`; `get_meta(key)` / `set_meta(key, value)`. Use `rivet_core` types where they exist (e.g. the parse status strings must be the documented set; add a `ParseStatus` enum in rivet-core `kinds.rs` if absent, with `as_str`: ok, parse_error, resource_limit, binary, size, encoding, unsupported).
5. Tests: schema creation sets format version 1 and `PRAGMA foreign_keys` is on; round-trip a file row with `source` containing bytes 0x00, 0xff, CRLF, and a UTF-8 BOM and assert byte equality and hash equality; `list_files` is byte-ordered regardless of insert order; deleting a file row with a dependent `symbols` row cascades (insert a symbol row directly via SQL for the test); an unknown `index_format_version` returns `IncompatibleIndexFormat` and leaves the file bytes unchanged (compare file hash before/after); a symlinked `rivet_dir` is rejected (`#[cfg(unix)]`); a symlinked `index.db` is rejected (`#[cfg(unix)]`).
6. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test -p rivet-store`. Paste real results.
7. Update docs/TASKS.md: tick T08, Last completed = T08, Next = T09, one-line "Last checks". Do not touch other docs. Do not run git.

Finish with the AGENTS.md final block.
