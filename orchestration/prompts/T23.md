Carry-forward from the T22 review (not a defect, a deferred assertion). T22 was asked to assert that the query path reports a use as unresolved after its target file is deleted, and correctly reported that no implemented command surfaces uses, since `refs` is this task. Now that you are building `refs`, add that assertion here: delete a binding target, re-index, and assert `refs` still reports the use, with `resolution: name_match` and `resolved_target: null`, rather than dropping it or erroring.

Implement T23 from docs/TASKS.md only. Read AGENTS.md first, then the T23 row; `docs/OUTPUT-CONTRACT.md` sections "Transport and common rules", "Flag applicability", "Coordinates and symbol objects", "Common index metadata", "Pagination and resolution", "`rivet refs <query> --json`", "Errors", and "Ordering and versioning"; spec §11.3, §11.5, §15, §20.4, §21; `docs/ARCHITECTURE.md` "Resolution and context"; and the existing `crates/rivet-cli/src/symbol.rs`, `transport.rs`, `index.rs`, plus `crates/rivet-store/src/lib.rs` read APIs. Nothing else.

`docs/OUTPUT-CONTRACT.md` is normative. Where this prompt and the contract disagree, the contract wins and you report the disagreement.

T23 — Wire the `refs` command: reference and candidate modes, filters, counts, and pagination.

Requirements:

1. Add `crates/rivet-cli/src/refs.rs` and `mod refs;` in `main.rs`. Follow the `symbol.rs` pipeline exactly: validate arguments before any filesystem work, open the context, validate configured languages, refresh (or use the cached store for `--no-refresh`), resolve the query, then assemble JSON with keys inserted in contract order. Reuse `symbol.rs`'s symbol-object builder for `symbol` and `containing_symbol` rather than writing a second one. Do not restructure `symbol.rs`; if you must make it shareable, move the helper with no behavior change and say exactly what you moved.

2. Flags, per "Flag applicability": `--json`, `--mode references|candidates` (default `references`), `--kind` (comma-separated `ref_kind` values, default all), `--min-resolution exact|scoped|name_match` (default `name_match`), `--limit N` (1..=1000, default 50), `--offset N` (default 0), `--freshness content|metadata`, `--no-refresh` (conflicts with `--freshness`). Reject unknown flags and out-of-range values before filesystem work, with `invalid_arguments` and exit 2. An explicit positional query is required.

3. Mode semantics are the substance of this task.
   - `references` (default): include uses bound to the queried declaration, and same-name uses that are unresolved. EXCLUDE uses bound to a different declaration. Alias uses reach the target through their binding even when their spelling differs from the target's name; they must be retained.
   - `candidates`: everything above, plus every extracted use whose normalized unqualified name matches the target's. A use bound elsewhere appears with `resolution: name_match` relative to this query while keeping its real `resolved_target` for inspection.
   - Declarations and literal text are never references. `tests/gold/php-authored.toml` has `[[not_a_use]]` spans; assert those never appear in either mode.

4. `name_match` is query-relative and must never be persisted. The `bindings` table rejects it by schema. Compute it at query time from the relationship between the use's stored binding and the queried target. Never rewrite a stored `resolution`.

5. Ordering, dedupe, counts, pagination, in this order: filter by kind and by minimum resolution, then sort by `(file bytes, start_byte, end_byte, ref_kind, resolved_target or empty)`, then count, then slice. Dedupe by `(file, start_byte, end_byte, ref_kind)`; a call site appears once as `call` and is never repeated as `unknown`. `total` counts all matches after filters. `truncated` is true when any match lies outside the page, including earlier pages. `next_offset` is the next offset when later results exist, else null. `by_resolution` counts all filtered matches BEFORE pagination and always carries exactly the three integer keys `exact`, `scoped`, `name_match`. An offset beyond the end is an empty page, not an error.

6. Reference object keys, in contract order: `file`, `content_hash`, `start_byte`, `end_byte`, `line`, `column`, `containing_symbol`, `ref_kind`, `resolution`, `resolved_target`, `receiver`. Note `line`/`column` singular and one-based; the store field is `col`. `containing_symbol` is a full symbol object or null, and a use inside an anonymous function inherits the nearest named container.

7. Errors reuse the existing `transport.rs` constructors and exit codes: `invalid_arguments` 2, `repository_unavailable` 3, `symbol_not_found` 4 with at most 5 suggestions, `ambiguous_symbol` 5 with `total`/`truncated`/`next_offset`/`candidates`. Match `symbol.rs`'s handling of each `QueryOutcome` arm.

8. Remove the not-implemented placeholder for `refs`: the `"T23"` arm in `main.rs`'s `task()` and the dispatch that routes `Refs` to `not_implemented`. `crates/rivet-cli/tests/index_json.rs::unimplemented_command_emits_json_error` currently asserts `refs` is unimplemented and WILL break. Retarget it to a command that is still unimplemented rather than deleting the coverage, and say which you chose.

9. Store access. There is no `find_bindings_by_target`. You may add one to `crates/rivet-store`, or load bindings once into a map keyed by `use_id`. Either way the result must be deterministically ordered; no `HashMap` iteration order may reach output. State which you chose and why.

10. Tests in a new `crates/rivet-cli/tests/refs_json.rs`, following the helper shape in `symbol_json.rs` and `bindings.rs`. Assert exact spans and target IDs, not just counts. Cover, each as its own test:
    a. Default mode on the authored fixture: exact byte spans of the included references, and that a use bound to a different same-name declaration is absent.
    b. Candidate mode on the same query: the excluded use now appears with `resolution: name_match` and its real `resolved_target` retained.
    c. An aliased import use is retained in reference mode though its spelling differs from the target name.
    d. `--kind` filtering, including a value that matches nothing, giving an empty list with `total: 0` and the three `by_resolution` keys still present.
    e. `--min-resolution scoped` retains exact and scoped and drops name_match.
    f. Pagination: a limit smaller than the match count sets `truncated` true and a correct `next_offset`; the second page continues without overlap or gaps; an offset beyond the end is an empty page with `truncated` true; `by_resolution` is identical on every page because it counts before pagination.
    g. `--limit 0`, `--limit 1001`, an unknown `--mode`, and an unknown `--kind` are each `invalid_arguments` exit 2 before any filesystem work.
    h. The carry-forward assertion from the top of this prompt.
    i. The gold `[[not_a_use]]` spans appear in neither mode.
    j. Determinism: the same query twice returns byte-identical JSON.

11. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results with exact per-crate test counts.

12. Update docs/TASKS.md: tick T23, Last completed = T23, Next = T24, one-line "Last checks". Do not touch other docs. Do not run git.

If this task grows beyond one coherent change, split the unfinished scope as T23a/T23b per the TASKS.md rule, record precisely what works and what remains, and do not tick T23. Report contradictions rather than silently resolving them.

Finish with the AGENTS.md final block.
