Implement T01 from docs/TASKS.md only. Read AGENTS.md first, then the T01 row and the "Read:" line for section A, then docs/BUILDING.md "Workspace layout" and docs/ARCHITECTURE.md "Workspace boundaries". Nothing else.

T01 — Create the local Rust workspace and runnable CLI shell.

Requirements:
1. Root `Cargo.toml` is a virtual workspace (no root package) with `resolver = "2"`, members `crates/*`, and `[workspace.package]` carrying `version = "0.1.0"`, `edition = "2024"` (fall back to 2021 only if the installed toolchain rejects 2024, and say so), `license = "MIT OR Apache-2.0"`, `publish = false`. Do not set `rust-version` yet; T02 pins it after the dependency spike.
2. Six crates under `crates/`: `rivet-cli` (binary named `rivet`), `rivet-core`, `rivet-parser`, `rivet-index`, `rivet-store`, `rivet-languages`. Each library crate has a `lib.rs` containing only a crate-level doc comment stating its responsibility (copy the one-line responsibility from ARCHITECTURE "Workspace boundaries"). No types, no functions, no dependencies between crates yet except `rivet-cli` depending on nothing beyond `clap`.
3. `rivet-cli` uses `clap` (derive) with `--version` and `--help`. Define the six MVP subcommands `init`, `index`, `symbol`, `refs`, `context`, `snippet` as parsed subcommands so `--help` lists them, but each one, when invoked, prints to stderr `rivet <cmd>: not implemented yet (see docs/TASKS.md)` and exits with code 1. Do not implement `--json` handling or any flags beyond the subcommand names. `rivet --version` prints `rivet 0.1.0`.
4. `.gitignore` already exists at the repo root and covers `target/`. Do not replace it; only append if something generated is missing.
5. Run: `cargo build -p rivet-cli`, `./target/debug/rivet --version`, `./target/debug/rivet symbol Foo; echo exit=$?`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`. Paste the real output lines in your final message.
6. Record the exact `rustc --version` and `cargo --version` output in the "Current checkpoint" table of docs/TASKS.md under "Last checks", tick the T01 checkbox, set "Last completed task" to T01 and "Next task" to T02.
7. Commit `Cargo.lock`? No: do not run git at all. Just leave `Cargo.lock` in the tree.

Do not create any scanner, parser, database, or language code. Do not add dependencies other than clap. Finish with the final message block from AGENTS.md.
