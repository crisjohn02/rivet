Implement T21 from docs/TASKS.md only. Read AGENTS.md first, then the T21 row, spec §11.3 (the `scoped` row) and §11.4 item 2 (including its "no intervening reassignment or uncertain control flow" condition); docs/ADDING-A-LANGUAGE.md "MVP support boundary" (PHP row Local receiver hints); the existing resolver in `crates/rivet-index/src/resolve/` including `rules/receivers.rs` from T20; and how `new_bindings` are recorded in scope facts by `crates/rivet-languages/src/php/uses.rs`. Nothing else.

T21 — Resolve preceding-`new` receiver hints to `scoped` bindings, conservatively. Add ONE rule file and register it. You may make minimal behavior-preserving edits to `rules/mod.rs` or other rule files ONLY to reuse an existing shared helper; if you do, state exactly what you changed and confirm the existing binding set is unchanged. Do not restructure the resolver.

Requirements:
1. Add `crates/rivet-index/src/resolve/rules/new_expr.rs`, registered LAST in `rules/mod.rs`. For a use with hint `NewExpr { class_spelling }`: resolve `class_spelling` with the shared scope-chain class-resolution helper T20 extracted (alias import, fully qualified, namespace-relative), require exactly one class, require that class to declare the member named by the use's spelling, and bind `scoped`.
2. Conservatism is the substance of this task. The binding holds only when the assignment is safe to trust. The extractor records `new_bindings` per scope; this rule must NOT bind when any of the following is true, and each needs a test:
   a. the receiver variable is assigned more than once anywhere in the same scope (reassignment), even if the second assignment is also a `new` of the same class;
   b. the assignment occurs after the use in source order;
   c. the variable is assigned from anything other than a direct `new` (a call, a parameter, a property read);
   d. the assignment is inside a conditional or loop body while the use is outside it, or vice versa (uncertain control flow).
   If `uses.rs` does not currently record enough information to detect (a), (b) or (d), extend the recorded `NewBinding` facts with what is needed (for example an assignment span, an assignment count per variable per scope, and the enclosing block node id or depth), keeping the change additive and re-running the gold checks. Say clearly in your report which of (a)–(d) are enforced by recorded facts and which, if any, you could not enforce.
3. Never `exact`. A variable whose class cannot be resolved to exactly one indexed class records nothing.
4. Tests. Unit tests in `new_expr.rs` for the happy path and for each of (a)–(d) individually. Extend `crates/rivet-cli/tests/bindings.rs`: on the authored fixture, `ReportService.php:24` (`$svc = new SurveySvc(); $svc->launch();`) binds `scoped` to `App\Services\SurveyService::launch`; `boot.php:17` and `boot.php:22` (the interpolated call) bind `scoped` to the same target; `ReportService.php:36` `$x->launch()` remains unbound. State and assert the exact total `bindings` count, and assert that NO binding has resolution `exact` for any receiver-based call.
5. Adversarial temp fixture: a scope where `$s = new A\Svc(); $s->go(); $s = new B\Svc(); $s->go();` — with reassignment present, neither call binds. A scope where `if ($c) { $s = new A\Svc(); } $s->go();` — no binding. A scope where `$s = new A\Svc(); $s->go();` with no reassignment — binds.
6. Run `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test --workspace`, and `python3 tests/gold/check_gold.py`. Paste real results.
7. Update docs/TASKS.md: tick T21, Last completed = T21, Next = T22, one-line "Last checks". Do not touch other docs. Do not run git.

Finish with the AGENTS.md final block.
